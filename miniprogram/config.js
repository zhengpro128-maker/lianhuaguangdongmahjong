/**
 * 验收环境的游戏 H5 地址。
 *
 * 正式发布必须是 HTTPS 业务域名并以 / 结尾。仅供开发者本人真机预览时，可暂填
 * http://公网IP:端口/，但开发者工具和手机微信都必须显式开启「不校验合法域名」调试。
 * example.invalid 仅用于防止误连生产环境。
 */
const GAME_WEB_URL = 'https://example.invalid/lianhua-guangma/'

module.exports = { GAME_WEB_URL }
