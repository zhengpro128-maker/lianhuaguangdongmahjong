# 莲花麻将血流：前端与 P2P 可执行实施方案

> 历史首版计划（E00～E09）。原勾选只记录当时的实现范围，不表示后续通用交互问题已修复，也不是现行规则或进度来源。规则已提取至 [rules.md](../rules.md)。
> 当前入口：[文档首页](../README.md) · [当前任务](../tasks.md) · [验收记录](../acceptance.md)。


更新：2026-09-05。初始基线：master `d784cbf`；最新提交与验收见验收记录；P2P 位于 `work/vibehub-theme11v`（执行前重新核对 HEAD）。

**状态：E02～E05、E07、E08 已实现；E06 本地 mock 验收通过，真实 SDK 待验收；E09 千局与双分支自动化已执行。单机默认开放，P2P 生产入口保持关闭。** 本文曾作为首版实施顺序与规则配置依据；[分支接线与基线](branch-integration.md)记录本批证据，[验收记录](../acceptance.md)记录提交与检查。[体验设计](experience-proposal.md)补充视觉细节，[数值实验](balance-prototype.md)保留历史数据。旧文档中的阶段编号、实验对照表和已被用户撤回的建议不能覆盖本文。

## 1. 执行范围与不可改动的边界

### 1.1 用户已明确的要求

- 只做前端单机和 P2P，不实现 Python 游戏后端、WebSocket 新玩法、新 TTS 后端或代理服务。
- 在现有莲花麻将基础上新增血流，增加第 3 节的 16 种常规番型；保留原特殊手。十三幺基础 16 倍，加入硬胡 ×2。
- 不增加双字顺、三字顺、四字顺、全顺、七字齐等新番型；现有字牌顺子仍是合法面子。
- 复用现有动作报声代码、音频资源及角色音色路由。不新增一套“我胡了”语音，不重写普通出牌 / 吃碰杠报声策略，不让同一动作重复播两次。
- 局内胡牌只有现有动作报声、番型、分数、特效和盖楼；首次胡、大番、多响、逆转均不触发感言或付款者反应。
- 全桌感言只在整局结束后启用，且仅限 `llm`、`llmAnime` 主题；其他三个主题无感言气泡、感言语音及相关 TTS 请求。
- “莲花”只表示地名与玩法来源；不派生花卉符号、地域风格或世界观。五主题沿用各自概念。

### 1.2 本版实施默认值

以下由前面的建议收敛成可编码的默认配置，不虚称用户已逐条确认；实现时集中配置，不分散写死。若用户调整，更新版本与测试预期，不另行猜测。

| 项目 | 本版默认 |
|---|---|
| 新规则键 / 显示名 | `lotus-blood-flow` / 莲花麻将·血流 |
| 启用范围 | 新血流模式；原 `lotus-classic`、`lotus-legacy` 不自动获得新计分 |
| 底分 / 初始分 / 封顶 | 10 / 2000 / 每次胡牌每位付款人最终 64 倍 |
| 首胡后 | 固定暗手和副露，不再换张、吃碰杠；只处理新摸牌的胡 / 过 / 摸切 |
| 普通点炮 / 自摸 | 点炮者分别向赢家付款；自摸由另外三家付款，已胡玩家也付 |
| 响应 | 胡优先，允许点炮多响及抢补杠多响；其余沿用杠 > 碰 > 吃 |
| 过胡 | 每窗口只响应一次；首版不加跨窗口过水限制 |
| 庄家 / 场制 | 无庄家计分倍率；局末轮庄；东风场 4 局、半庄场 8 局 |
| 负分 | 允许继续，不截断应付、不按负分提前淘汰 |
| 附加规则 | 不加买马、跟庄、定缺、换三张、查叫赔付、退税、呼叫转移 |
| 房主永久退出 | 不承诺无缝迁移；恢复失败则显示中断，保留最后已确认流水 |

“整局结束”指本局牌墙耗尽且最后响应完成，不是任何一家胡牌，也不是必须等整场 4 / 8 局全部结束。

### 1.3 现有行为需要保留

- 保留 136 张实体牌、两次骰子、双精和白板受限替代。当前前后端均移出整墩两张，实际发摸序列为 134 张；指示牌与底张分别参与守恒，底张不公开。
- 保留数牌吃、乱风吃、三元吃、普通杠与风杠。精牌打出或被抢时只按本张参与本次胡；对方暗手中原有精牌仍可替代。
- P2P 已有房主主题同步及客机主题锁定，本轮不新增混主题房间。感言使用本机已生效的房间主题判断，以防迟到事件；不另建主题同步机制。
- LLM 决策、普通动作音与感言是不同通道。关闭感言不得关闭 LLM 打牌或静音全部动作。

## 2. 工作方式、文档优先级与阶段依赖

第一版执行顺序：E00 → E01 → E02 → E03 → E04 → E05 → E06 → E07 → E08 → E09。

第一版后的游戏表现差距按[第二轮表现补齐计划](../design/presentation.md) E10～E16 执行，覆盖结算按钮与最终排名、共享胡牌节奏、飞牌落楼、多响收付、五主题打击感和局末感言位置。它不重做已完成的规则，也不把 E07 的“基础接入完成”视作所有观感目标已完成。当前按钮导航修复为 master `fc86da7`；此处为第二轮立项时记录；后续提交状态见当前任务表。性能、安全及 AI 策略调整不属于这份表现补齐清单。

后续澄清：E11 的统一节奏包含 llmAnime 获胜角色立绘；E14 明确接入非人声胡牌效果音并验收原动作报声实际可听；E15 保留正常摸打 / 吃碰杠的既有 LLM 短句气泡，修正第一版“所有局内 message 都忽略”的过度屏蔽。只有局内胡牌自由感言继续禁止，全桌感言仍只在两个 LLM 主题的局末启用。详见第二轮计划第 1.1 节，原动作音资源与角色路由不替换。

- 新文件名是计划路径；已有文件以当前代码为准，重命名时同步本文与测试路径。
- 进入每阶段先读其依赖和验收项；完成后记录提交、命令、退出结果与必要证据，再勾选清单。
- 不将“文件已创建”“mock 通过”“同步成功”当作玩法可用。只有阶段验收全部满足才标为完成。
- 所有共享 UI / 规则先在 master 提交，按 AGENTS.md 执行 `pnpm sync:vibehub`；P2P 独有接线与共享改动分别提交。不得提交无关工作。
- 本文不授权发布现网。E09 产出可发布包和验收记录，真实发布只在已有明确授权范围内执行。

| 阶段 | 主要产物 | 依赖 | 当前状态 |
|---|---|---|---|
| E00 | 分支接线清单、工作树同步方案、动作音基线 | 无 | 完成；三处具体接线已获用户授权 |
| E01 | 规则配置、数据契约、黄金用例 | E00 只读盘点完成 | 完成；黄金输入已由实际评分器验证 |
| E02 | 完整判型与硬胡评分器 | E01 | 完成；浏览器 Worker 已装配 |
| E03 | 血流状态机、多响与账本 | E02 | 完成；共享权威引擎已接入单机和 P2P |
| E04 | 规则 AI、单机入口和听牌提示 | E03 | 完成；单机默认入口开放 |
| E05 | 胡牌楼、番型卡、局末流水 | E04 | 完成；五主题与四种尺寸已验收 |
| E06 | P2P 权威接线、恢复与一致性 | E05；E00 接线约束已解决 | 本地验收通过；真实 SDK 待验收 |
| E07 | 五主题分级特效 | E05、E06 稳定事件 | 完成；队列、原声音和小屏回归通过 |
| E08 | LLM 胡 / 过、预算与局末感言 | E04、E06、E07 | 完成；本地和 P2P 模型 mock 通过 |
| E09 | TypeScript 模拟、联调、回归与交付 | E01～E08 | 千局、三策略分布、硬件 GPU 与自动化回归完成；真实 SDK 待验收 |

## 3. 规则与计分规范

### 3.1 番型目录

除单独说明外，以下条件都在同一合法分解中判断，包含全部副露、将牌和本次胡牌张。普通同牌杠可以替代刻子；风杠不能替代同牌刻子。

| ID | 名称 | 基础倍数 | 判定 |
|---|---|---:|---|
| `pure-suit` | 清一色 | 4 | 恰好一种数牌花色，无字牌 |
| `mixed-suit` | 混一色 | 2 | 恰好一种数牌花色与字牌同时存在 |
| `all-triplets` | 碰碰胡 | 2 | 四副同牌刻 / 普通杠加将；无数牌顺、字牌顺、风杠 |
| `little-three-dragons` | 小三元 | 4 | 两种箭牌刻 / 普通杠，第三种箭牌作将 |
| `big-three-dragons` | 大三元 | 8 | 中发白三种各一副刻 / 普通杠 |
| `little-four-winds` | 小四喜 | 8 | 三种风刻 / 普通杠，第四种风牌作将 |
| `big-four-winds` | 大四喜 | 16 | 四种风牌各一副刻 / 普通杠 |
| `nine-gates` | 九莲宝灯 | 16 | 无任何吃碰杠；同花色 `1112345678999` 加同色任意一张，按最终牌形判定 |
| `all-green` | 绿一色 | 16 | 仅二三四六八条与发，组成合法和牌；不强制有发 |
| `pure-terminals` | 清幺九 | 16 | 仅数牌一九的四副刻 / 普通杠加将 |
| `mixed-terminals` | 混幺九 | 4 | 仅数牌一九与字牌的四副刻 / 普通杠加将，数牌和字牌均须有 |
| `three-concealed-triplets` | 三暗刻 | 4 | 至少三副暗刻 / 普通暗杠；其余面子可为顺子 |
| `four-concealed-triplets` | 四暗刻 | 8 | 四副暗刻 / 普通暗杠加将 |
| `all-honors` | 字一色 | 8 | 完整和牌全为字牌，允许现有字顺与风杠 |
| `three-kongs` | 三杠 | 8 | 至少三副已成功声明的四张同牌杠，另有合法面子和将 |
| `four-kongs` | 四杠 | 16 | 四副已成功声明的四张同牌杠加将 |

原特殊手：平胡 1、七对 2、十三烂 2、七星十三烂 4、十三幺 16。七对保持当前合法性口径，可叠牌种属性；十三烂、七星十三烂、十三幺作为独立特殊手，不追加面子 / 牌种番，但可按实际自然成立条件获得硬胡。

### 3.2 精牌与硬胡

- 可替代分解：精牌可代表任意牌；白板仅可代表本局精牌牌面或白板本身。已落地的吃碰杠不重新解释。
- 自然分解：完全按真实牌面成立，精牌与白板均不用替代能力，硬胡 ×2。物理上含精不等于一定软胡。
- 各分解分别完成番型组合、硬胡、自摸等事件与封顶后，取最高合法支付；同分优先自然分解，再按稳定番型 ID 顺序，确保两端显示稳定。
- 不能从分解 A 取得硬胡资格，再给分解 B 的替代高番 ×2。
- 外来精牌只消费该张牌的普通身份；不能将暗手中所有同种精牌一起降为普通牌。
- 虚拟代表牌不改变实体牌库数量。当前规则允许的绝张替代口径保留；不得在新评分器内顺手改变旧模式合法性。
- 每次血流胡牌都重算分解、硬胡与事件标记，不继承首胡标签。

### 3.3 明暗与杠的边界

- 点炮 / 抢杠牌补成刻子时，该刻不算暗刻；补将不影响另外四副暗刻。按本次胡牌张的合法归属分别评分。
- 普通暗杠算暗刻，碰、直杠、补杠不算；不能以 UI 是否亮牌判断明暗。
- 三杠 / 四杠只数已提交普通杠：风杠、手中四张未声明、pending 补杠、被抢的补杠均不计。碰升补杠仍是一副。
- 风杠仍占一个结构面子，有四张字牌实体和原有即时杠分，但不算暗刻、同牌刻或本轮三 / 四杠。

### 3.4 组合、事件与支付

排除关系：四暗刻包含三暗刻和碰碰胡；四杠包含三杠和碰碰胡；大四喜、清幺九、混幺九包含碰碰胡；九莲包含清一色。大小三元、大小四喜按定义互斥。小四喜和大三元不必全刻，因此实际满足时允许另计碰碰胡。

计算步骤：

1. 每个分解匹配番型，去除被包含项；`M = 1 + Σ(weight - 1)`。
2. 事件倍率 E：普通点炮 1，自摸 2，抢普通补杠 2，杠后自摸 4。它们按来源互斥分支取值，不重复贴自摸标签收三家钱。
3. 天胡 / 地胡默认与本次普通结果择高：该分解的事件后倍率 `Q = max(M × E, 8)`；非开局事件 `Q = M × E`。开局加成不另行连乘，首胡批次结束后关闭后续天胡 / 地胡窗口。
4. 该分解自然成立时 H=2，否则 H=1。单家支付 `10 × min(Q × H, 64)`。
5. 每位赢家选择合法最高支付；点炮 / 抢杠只向来源玩家收，自摸向另外三家收。多响统一提交聚合收付向量。
6. 即时杠分单独结算，不乘硬胡或重复胡次数：直杠来源付 B，补杠其他三家各付 B，暗杠 / 风杠其他三家各付 2B。

算术验收：硬胡十三幺点炮 320，自摸每家 640 / 总收 1920；硬胡清一色 + 碰碰胡自摸为 20 倍，每家 200 / 总收 600。已经封顶后不再额外翻倍。

## 4. 状态与协议契约

新类型建议放 `src/game/variants/lotus/bloodFlow/types.ts`，以可选能力接入 `GamePort`，不破坏旧模式必需字段。

| 类型 | 必备字段 / 责任 |
|---|---|
| `BloodFlowRuleConfig` | 规则 ID、版本、权重、硬胡、事件倍率、封顶、锁手及场制 |
| `WinEvaluationInput` | 暗手、完整副露、胡牌张及来源、精牌上下文、开局 / 杠摸标记 |
| `WinEvaluation` | 最佳分解、计分项、排除理由、精牌分配、自然成立证据、封顶前后倍率、支付 |
| `ClaimWindow` | 局 ID、窗口 ID、状态版本、来源牌事件、候选席位、独立决定、统一截止时间 |
| `WinBatch` | 批次 ID、来源事件、来源席位、赢家数组、四家 delta、提交后分数、下一动作信息 |
| `WinRecord` | 赢家、序号、源牌引用、公开番型和倍率、收付；完整暗手证据仅权威端保存 |
| `BloodFlowSeatState` | 胡牌次数、锁定标记、首次胡序号、记录引用 |
| `BloodFlowRoundResult` | 结束原因、开局 / 结束积分、胡杠净分、胡牌统计、排名、公开流水 |

### 4.1 事件不变量

- 一个源牌事件最多归档一张实体牌；多响有多个展示记录引用，不能复制牌库实体。
- 一个窗口内每席最多胡一次；重复消息、迟到 AI / LLM 结果或超时回调不能再次提交。
- 每次支付四家之和为零；整局净分等于全部已提交胡牌与杠分。
- 136 张物理牌守恒；翻精两张、牌墙、暗手、副露、弃牌和胡牌归档均纳入。有效手牌张数按暗手 + 3 × 结构面子数判断，杠的额外实体单列。
- 公共事件不含暗手、完整精牌分配或牌墙顺序；全局权威输入不能直接送给 AI / LLM。
- 新模式局内不能调用旧 `endGame` 终局时间线、不能设置全桌 `revealHands=true`、不能发送 `round_settled`。
- 动画与声音不负责积分提交、发牌、锁手或推进规则。

### 4.2 续行表

| 当前事件 | 提交后实体与手牌 | 下一动作 |
|---|---|---|
| 自摸 | 本次摸牌归档，剩余暗手锁定 / 保持锁定 | 赢家下家摸牌 |
| 点炮多响 | 弃牌只归档一次，各赢家手牌不永久加牌 | 出牌者下家摸牌 |
| 抢补杠多响 | 原碰保留，第四张归档；不补牌、不收该次杠分 | 被抢者下家摸牌 |
| 无人胡 | 原首版按第二轮询问吃碰杠；该交互已被 7ca6dbb 的同窗候选修复替代 | 按既有动作规则 |
| 锁手后自摸过胡 | 仅能打出本次摸牌，其他人照常响应 | 正常弃牌流程 |
| 最后摸牌 / 弃牌 | 完成所有本次胡响应；无牌可补时禁止新杠 | 结算本局；不再开启无意义的吃碰循环 |

天胡用开局明确记录的第 14 张归档。最后一张牌有人胡也必须把批次结算完成，再做整局汇总，不能漏掉最后多响。

## 5. E00：分支、接线与基线盘点

目标：执行者在改游戏前知道哪些文件能共享、哪些被同步脚本恢复，以及如何同步当前工作树。

- [x] E00-1 记录 master / vibehub HEAD、`git status --short`、`git worktree list`；保留用户已有改动，不自动 stash、清理或移动工作树。
- [x] E00-2 检查 `scripts/check-vibehub-ahead.ps1`，审查共享文件真实领先修复；脚本仅警告后继续时，不跳过人工代码审查。
- [x] E00-3 将受影响文件与 `$vibehubKeep` / `$masterOnly` 对照，形成 `docs/blood-flow/archive/branch-integration.md`：逐文件记录 master 职责、P2P 职责、接入方式和同步后检查。
- [x] E00-4 处理 vibehub 已在独立工作树签出的同步约束。优先将同步脚本改为识别并在已有干净目标工作树完成合并 / 恢复 / 提交，保留原有 keep 语义；不得用强制签出、移除工作树或覆盖脏文件解决。
- [x] E00-5 固定动作音回归样例：旧两模式、五主题、真人 / 规则 AI / LLM、自摸 / 点炮 / 抢杠。记录当前路由与每次播放次数，不修改音频内容。75 个模块级场景已通过；classic 单机普通点炮不适用，真实实听留 E07/E09。
- [x] E00-6 运行一次现有前端基线检查，分开记录既有失败和本轮引入失败，不将无关问题掺入实现。

**P2P 受保护文件的明确约束：** 当前 AGENTS.md 禁止手动修改 P2P 的若干壳层 / 编排文件；`gamePort.ts`、`lotusGame.ts`、协议和结算组件也在同步 keep 清单中。先检查是否可通过现有注入点和新增共享模块接入。若必须修改受禁止的既有接线，E00-3 应先给出具体路径、最小 diff 及保留 P2P 职责的方式，再取得针对该范围的工作流补充授权；不能用“脚本代写”规避限制、假定旧 Phase 11V 授权覆盖本任务，或直接删 keep 项。此时 E01～E05 可继续，E06 接线不冒险推进。

完成证据：分支接线清单、同步目标与脏工作区处理规则、动作音基线结果。E00 不以“已经运行 merge”作为唯一完成标准。

## 6. E01：配置、类型与黄金用例

文件：新增 `variants/lotus/bloodFlow/config.ts`、`types.ts`，`variants/lotus/patterns/types.ts`、`fixtures/*.json`；按需要扩展 `core/rules/ruleset.ts` 的可选上下文。P2P 受保护端口接线留到 E06。

- [x] E01-1 将第 3 节唯一配置落成 `lotus-blood-flow-v1`，分离原两模式配置。所有金额使用整数。
- [x] E01-2 落成第 4 节契约；区分私有评分证据和公共展示结果。
- [x] E01-3 为 16 番、原特殊手、硬胡、叠加和来源差异编写表驱动黄金用例，包含明确预期而非只对比自身输出。48 个实体手牌输入、13 个计分择优输入；并非评分器已验收。
- [x] E01-4 用例进入受版本控制的 `src/` 或 `tests/`，不能依赖被忽略的 `work/lotus-balance/`。历史 Python 样本仅作参考，不能直接作为生产评分器。

验收：16 个新增 ID 唯一且权重齐全；十三幺为 16；硬胡为修饰项；旧规则默认不变。提交建议：`feat: define blood-flow rules and contracts`。

## 7. E02：完整判型和评分器

文件：新增 `variants/lotus/patterns/{decompose,catalog,evaluate,score}.ts` 与对应测试；从 `lotusRules.ts` 复用 / 抽取精牌、字顺和特殊手纯逻辑，保持旧导出兼容。

- [x] E02-1 实现标准面子与七对等合法分解，包含完整副露和胡牌张归属。
- [x] E02-2 实现可替代与完全自然两条评估路径，按最终支付择优，返回可解释证据。
- [x] E02-3 实现 16 番、原特殊手路由、包含关系、事件倍率和封顶；同一分解才可叠加。
- [x] E02-4 将自摸听口和点炮听口分别求值；外来精牌只按本张。
- [x] E02-5 使用计数缓存、等价分解去重；性能不够时优化或移 Worker，不截断合法解后悄悄少算分。

验收：全部黄金用例通过；硬胡十三幺点炮 320 / 自摸每家 640；精牌按本张的自然胡成立；替代高番不能借自然低番加硬胡；三 / 四杠不误计风杠或 pending。旧 `lotusRules.test.ts` 等规则回归通过。提交建议：`feat: evaluate blood-flow patterns and hard wins`。

## 8. E03：血流状态机、响应窗口与账本

文件：新增 `variants/lotus/bloodFlow/{state,claimWindow,winBatch,ledger,roundLifecycle}.ts`；接入 `lotusState.ts`、`lotusTurnOrchestrator.ts`、`lotusTileFlow.ts`、`lotusKong.ts`、`lotusSettlement.ts`。保留旧流程分支。

- [x] E03-1 拆开 resolveWinBatch、applyWinBatch、finishRound；局内不走旧终局副作用。
- [x] E03-2 收齐候选玩家胡 / 过，绑定窗口与版本，统一截止；补杠无人抢后才成功收分。
- [x] E03-3 维护源牌归档、逐席记录、锁手和开局分；按续行表处理最后一张、天胡与多响。
- [x] E03-4 所有异步回调核验状态版本；停止旧窗口只取消该窗口工作，不能误清整局记录。
- [x] E03-5 局末从账本汇总净分，不重放历史扣分；轮庄和场次只推进一次。

验收：固定牌墙分别复现自摸后再胡、二响 / 三响、抢杠回退、已胡仍付款、最后一张多响；逐动作零和与实体守恒；不出现重复牌或重复付分。提交建议：`feat: add continuous blood-flow rounds and ledger`。

## 9. E04：规则 AI、单机装配与听牌提示

文件：`lotusGame.ts`、`lotusControllers.ts`、`lotusAi.ts`、`lotusSelectors.ts`、`lotusHuman.ts`、`core/rules/ruleVariants.ts`、master `App.vue` / 大厅接线；新增血流策略模块，按场景过滤玩法列表。

- [x] E04-1 按规则配置创建单机引擎，保留旧两模式；新规则键未知时不静默当旧玩法。
- [x] E04-2 AI 支持首胡前做牌、首次胡 / 过取舍、锁手后强制动作；用座位可见输入，禁止读取其他暗手和墙顺序。
- [x] E04-3 人类首次可胡展示本次番型 / 得分及锁手说明；锁手后不展示非法吃碰杠操作，保留合法过胡。
- [x] E04-4 听牌提示分别显示自摸 / 点炮分值；公开剩余张数对多响源牌去重。
- [x] E04-5 开发阶段用显式功能开关开放单机测试；master 的旧 WS 房间入口不得提交新规则键。

验收：单机固定种子完整东风场及半庄场完成，负分可继续；AI 错误时能确定性兜底；原两模式与原动作音仍可用。提交建议：`feat: integrate blood-flow local play and bots`。

## 10. E05：番型卡、胡牌楼和局末流水

文件：新增 `components/table/BloodFlowWinCard.vue`、`components/settlement/BloodFlowRoundLedger.vue`、`components/table/three/bloodFlowWinPile.ts`；接入 `GameTableHud.vue`、`tableTilePresenter.ts`、`tableRenderTypes.ts` 与 master 结算壳层。

- [x] E05-1 番型卡显示实际计分主番、修饰、最终倍率和收支；其他项可展开，不把被包含项伪装成加分项。
- [x] E05-2 按用户后续确认，四家沿原胡牌位持续向上盖楼；桌面每层 4 张、移动每层 3 张，不再限制 3 / 2 层，不再把后续记录转为收纳；显示实际胡牌次数和层数。
- [x] E05-3 一条 WinRecord 一次楼层增长，显示真实胡牌牌面；精牌解释不替换展示实体牌面。
- [x] E05-4 一个源牌三响时三座楼各有记录引用，实体账仍只有一张；布局数据不参与规则计数。
- [x] E05-5 点击查看公开流水；局末显示全局胡杠净分和排名。重建时直接落到当前状态，不补播历史动画。

验收：四家各 0 / 1 / 4 / 12 / 40 / 80 条记录，横屏小尺寸与桌面均不遮手牌和按钮；座位旋转后来源、赢家、楼的位置都正确；局内无全桌暗手泄漏。提交建议：`feat: show blood-flow wins piles and round ledger`。

## 11. E06：P2P 权威接入与恢复

先确认 E00 分支 / 接线约束已经解决，再进入相应 P2P 文件。主文件为 `online/host/{hostGameRunner,localStateToSnapshot,lotusRemotePlayerController}.ts`、`online/useVibeRemoteGame.ts`、`online/protocol/*`、远程快照编排和 `online/vibe/{vibeLlm,matchStatsRecorder}.ts`。

- [x] E06-1 房主注入同一 TypeScript 引擎，新增规则版本协商与能力检查；旧客户端拒绝新模式，不能错误降级。
- [x] E06-2 添加可补发 win_batch 和按席位脱敏快照；包含 authorityEpoch、sequence、roundId、windowId、batchId。
- [x] E06-3 客机动作绑定身份、席位和当前窗口，房主验证；客机不自行算最终积分。
- [x] E06-4 先到增量 / 先到快照两种顺序均幂等；更新比分后不再叠旧 delta；source ID 不随座位旋转而变化。
- [x] E06-5 普通玩家重连恢复锁手、楼层、账本与当前操作；保留现有恢复宽限期。不得把短暂 Relay 切路立即当永久离席。
- [x] E06-6 本局结束才发送 round_settled 与完整亮牌，才累计完成场次；中途每次胡不增加完成场次。
- [x] E06-7 房主短断线暂停新动作；永久退出恢复失败显示中断，不伪造正常完局和排名奖励。
- [x] E06-8 沿用房主主题同步 / 锁定，不新增混主题功能，不改变模型密钥私有边界。

- [ ] E06-9 真实 VibeHub 云端 / WebRTC / 公网 Relay 冒烟（本地 DEV 仅走 mock，尚未验收）。

验收：本地 P2P mock 四端完成一局并比较逐批账本；两真人 + 两 AI 跑完整场；再做真实 SDK 网络冒烟，mock 结果不能替代真实链路。未知 / 重复 / 乱序 / 迟到消息不重扣分、不重盖楼。提交建议：`feat: integrate blood-flow P2P authority and recovery`。

## 12. E07：五主题分级特效与原动作音复用

文件：新增共享血流表现调度模块；接入 `winEffectPresenter.ts`、主题 presentation token、原 TableAction / 动作音出口。原 `animeAudioPolicy.ts`、`animeFixedTtsExecutor.ts`、`useAudio.ts`、音频资源以回归保护为主，不改台词和路由语义。

- [x] E07-1 稳定的批次队列与去重；同屏最多一个中央大标题，最多四个席位短反馈。
- [x] E07-2 按实际主番基础档位做普通 / 中番 / 大番 / 顶级四档；硬胡与自摸为辅助标签，封顶有明确提示。
- [x] E07-3 首次大番完整演出约 1.2～1.6 秒，相同重复胡使用 350～500ms 紧凑表现，完整演出建议 8 秒冷却；更高档可升级。
- [x] E07-4 多响只一个公共开场，各席位卡与落楼可错峰；积压时合并过期视觉，不丢业务记录。
- [x] E07-5 原动作报声只由既有出口播放，新视觉 presenter 不再调用一遍 hu / zimo，也不生成新“我胡了”音频。接入批次的必要事件桥接保持一次动作一次原报声语义。
- [x] E07-6 动作窗口由权威节拍开放，初始胡牌节拍约 450ms；开放后才启动倒计时。特效可局部收尾，不等四台机器动画 / TTS 结束。
- [x] E07-7 适配五主题、低画质、静音、reduced-motion、资源失败、切换 / 卸载清理；后四项不能阻断对局。

验收：已有动作音快照 / spy 次数与 E00 基线一致；二次元固定报声与普通报声不双播；同一事件重放不重复。胡十次、大番三响、掉帧及移动布局均保留牌型和分数可读性。参考素材只用于视觉节奏，未逐帧核对的参数不能写成竞品实测。提交建议：`feat: present tiered blood-flow wins with existing audio`。

## 13. E08：LLM 决策与仅局末感言

文件：`game/llm/{schema,candidates,prompt,llmController,conditionalReasoning,runtime}.ts`、局末感言调度；P2P 对应 `vibeLlm.ts` 与运行入口。不要通过改全局普通动作语音策略实现感言时机限制。

- [x] E08-1 血流首胡前提供胡 / 过候选，不沿用“能胡立即 return win”的新模式短路；候选由 E02 / E04 给出当前得分、硬胡、听口及锁手影响。
- [x] E08-2 无选择的强制动作不请求模型；有价值分支才请求。输入只含该 AI 手牌和公开信息，不含完整牌墙、对手暗手或私有评分证据。
- [x] E08-3 房主为每个 AI 请求一次；其他客户端不重复请求。返回后核验 epoch / 局 / 窗口 / request ID，超时、非法或过期由规则 AI 处理。
- [x] E08-4 默认普通决策预算约 2～3 秒，关键胡 / 过约 4～5 秒，含排队 / 重试且小于权威截止时间；尊重已有明确用户设置，预留兜底时间。P2P 同一多响窗口不串行等每人完整超时。
- [x] E08-5 局内所有胡牌事件禁止独立感言请求和自由评价；只复用原动作报声。不因模型附带 message、important 或 mandatory 绕过。
- [x] E08-6 真正局末且主题为 llm / llmAnime，才安排全桌感言；按本局净分及公开结果，每个有配置的席位最多一句，固定顺序，可跳过。
- [x] E08-7 其他三主题不显示感言气泡、不请求 / 播放感言 TTS。进入下一局、退出、主题变化时取消队列及迟到回调；不取消仍合法的打牌决策。
- [x] E08-8 复用已有 LLM 连接和可选 TTS；不可用时规则 AI / 原动作音继续，不新增 Python 服务。局内报声不套用既有长语音 midpoint 等待屏障。

验收：五主题 × 局内 / 局末矩阵；同一胡批次无感言网络请求；P2P 不重复生成；TTS 失败 / 静音不挂起；原动作音回归继续通过。提交建议：`feat: add blood-flow LLM choices and round-end reactions`。

## 14. E09：模拟、联合验收与交付

文件：新增可受版本控制的 TypeScript 无头模拟入口及报告生成器；相关 E2E 用例；`docs/blood-flow/records/validation-v1.md`。报告中注明规则版本、提交、种子、策略、样本量与局限。

- [x] E09-1 用实际 TypeScript 引擎重新跑含硬胡、十三幺 16 倍的固定种子模拟；至少先完成 1000 局守恒与停滞检查，再按稀有番型样本不足情况补条件压力。
E09-2 已补随机合法动作、生产规则 AI、首胡单家至少 40 分策略各 100 局固定种子分布，详见策略报告；只作分布与守恒验收，不声称 LLM 强度或长期平衡已证明。

- [x] E09-2 区分独立成牌次数、同手重复胡、条件大牌样本；记录首胡墙长、分差、封顶率、硬胡占比、各策略表现、整局长度。不得把历史无硬胡 Python 结果当作新版本平衡证明。
- [x] E09-3 完成单机东风 / 半庄、P2P 混合真人 AI、LLM 可用 / 不可用、重连、主题锁定与局末感言实测。
- [x] E09-4 五主题至少覆盖 568×320、844×390、1280×720、1920×1080。记录实际 GPU / 浏览器和帧耗时，不写未实测的“稳定 60fps”；高低档队列均可恢复。
- [x] E09-5 执行 typecheck、完整单测、build、相关 E2E；检查旧两模式、原动作音、旧 WS 入口和 P2P 同步保留文件。
- [x] E09-6 所有新共享文件提交后执行规定同步，分别记录 master / vibehub 测试。开关默认只在已通过验收的单机 / P2P 入口开启。
- [x] E09-7 交付提交列表、测试结果、规则版本、已知限制、构建产物与回退方法。未获发布授权时到此结束，不自动部署现网。

## 15. 必测场景编号与失败处理

| 编号 | 覆盖 | 主要阶段 |
|---|---|---|
| R01 | 16 番正例 / 缺条件反例、实际副露破坏牌种、精与白板受限替代 | E01～E02 |
| R02 | 硬胡自然 / 替代择优、精按本张、白板按本张、封顶前 ×2 | E02 |
| R03 | 点炮补刻 / 补将、暗杠 / 补杠 / 风杠、九莲杠排除 | E02 |
| F01 | 首胡后再胡、已胡付款、锁手非法动作、天胡第 14 张 | E03 |
| F02 | 二响 / 三响、有人过 / 超时、无人胡后吃碰、最后一张多响 | E03 |
| F03 | 抢补杠回退、pending 不计杠、杠分一次、每步零和与 136 张守恒 | E03 |
| U01 | 四家持续盖楼 / 座位旋转、源牌去重、重建不补播 | E05 |
| N01 | 重复 / 乱序 / 缺号、私有快照、重连、未知版本、房主中断 | E06 |
| V01 | 同番连续 10 次、三个不同大番多响、低帧率、资源失败、五主题 | E07 |
| A01 | 原 hu / zimo 及固定角色出口不变、一次动作不双播、无新增替代报声 | E00、E07～E09 |
| L01 | 胡 / 过候选、合法性、截止时间、队列预算、迟到结果、无暗手泄漏 | E08 |
| S01 | 局内无感言请求、仅两主题局末感言、换局 / 静音 / 失败不阻塞 | E08 |
| C01 | 原两模式、原 WS、P2P 主题锁定、同步后 keep 文件语义 | 各集成阶段 |

每阶段先跑相关用例，阶段出口再按下表执行检查。发现失败先定位本阶段改动；未通过不开放新入口。不得为了测试通过删守恒断言、忽略协议错误或把新模式退化成旧模式。

| 检查 | 执行位置 | 用法 |
|---|---|---|
| 分支基线 | master 根目录及 P2P 工作树 | `git status --short`、`git worktree list` |
| 类型与单测 | 各分支对应根目录 | `pnpm typecheck`、`pnpm test` |
| 构建 | 各分支对应根目录 | `pnpm build` |
| UI / P2P E2E | 相应分支，用其自己的配置与服务 | `pnpm test:e2e`，先相关用例，再阶段出口回归 |
| 同步 | E00 同步前提已满足且 master 干净 | `pnpm sync:vibehub`，完成后验证 P2P，不附带 Push |
| 文本差异 | 对应分支 | `git diff --check` |

长运行网络测试使用 mock LLM / TTS 验证失败与延迟，不必消耗真实模型费用；真实服务只做有限连通与实际体验验收。没有真实 P2P 账号 / 网络环境时，可以完成前置工作并准确标注该项未验收，不能用 mock 替代勾选。

## 16. 完成清单与回退

- [ ] 新玩法单机默认入口已开、P2P 开发入口可用；真实 SDK 验收前不开 P2P 生产入口。原两模式与旧 WS 隔离已验证。
- [x] 16 番、十三幺 16 倍、硬胡 ×2、同分解计分与封顶均可解释。
- [x] 四家多次胡、多响、抢杠、锁手、末张和胡杠总账通过。
- [x] 四家盖楼可持续增高、可查账、可重连；实体计数不读取展示牌数量。
- [x] 五主题高番特效可辨识，重复反馈不阻塞操作。
- [x] 动作报声复用原代码与资源，无替换、无双播；只增加必要的新事件接线。
- [x] 局内不触发感言；仅 llm / llmAnime 在本局结束后有全桌感言。
- [x] LLM 的合法候选、预算、私有输入、兜底与原用户设置均通过。
- [ ] P2P 版本、幂等、恢复、主题锁定及同步在 mock 通过；真实 SDK 仍待验收。
- [x] 新 TypeScript 模拟包含硬胡，报告没有沿用旧实验结论冒充通过。
- [x] 验收报告记录两个分支提交、测试证据与限制，发布状态明确。

回退：优先关闭新模式入口并维持正在进行房间的规则版本；客户端不得把进行中的血流房间降级成旧玩法。再按对应提交回退新模式接线，保留旧端口和音频资源。P2P 已开房双方版本不匹配时明确拒绝继续，不能各算各的分数。

实施记录模板：阶段 / 状态 / master 提交 / vibehub 提交 / 测试命令与结果 / 证据路径 / 未解决项。证据摘要放受版本控制的文档；`docs/evidence/`、`work/`、`test-results/` 在当前仓库被忽略，大型产物放这些目录时不能声称它们已随提交交付。

本批已交付 E02～E09 的评分、规则引擎、单机与 P2P 适配、五主题展示、模型决策、局末感言、千局与浏览器回归。详见验收记录、千局报告和渲染测量。真实 SDK 仍未勾选；三策略固定种子指标已补齐；未部署。用户后续要求 headless 使用 GPU，已采用完整 Chromium headless + --enable-gpu，并断言实际渲染器不是 SwiftShader。
