# 胡牌演出与结算实施细则（E10～E16）

本文只维护任务定义和通过条件，不维护进度勾选。状态在[当前任务](../tasks.md)，规则在[规则与体验约定](../rules.md)，证据在[验收记录](../acceptance.md)。

优先参考：[843049425-1-208.mp4 的分析与片段](../references/impact-video.md)。下文的问题表是第二轮立项时的观察，不是当前缺失列表。后续实施以[当前任务](../tasks.md)的五个执行包为顺序，保留已经恢复的单机节奏、光柱、听牌网格及头像次数。

## 0. 实施前必须核对优先参考视频

这是特效实现的前置工作，不是可略过的阅读建议。执行者先打开原视频 [843049425-1-208.mp4](D:/Documents/Downloads/843049425-1-208.mp4)，通览事件分布，再实际查看下面的连续片段；可结合慢放或抽帧，但仅阅读本文、字幕或先前的总结不算看过。

| 必看片段 | 核对内容 |
|---|---|
| 1:04～1:10 | 点炮胡：来源牌、胡 / 过操作、角色确认、楼位强调、两家收付 |
| 1:16.5～1:19.8 | 高番文字：字形轮廓、厚度、透视冲入、短重拍、回弹和阅读停留 |
| 1:59～2:03.5 | 自摸：新摸牌、角色自摸、主番、落楼、三付一收的交接 |
| 5:44.5～5:48.5 | 本家放枪：本家来源、赢家席位、正负反馈的归属 |
| 5:49.5～5:54 | 一炮多响：共享提示在先，分席位动作 / 主番在后，最后共同收付 |
| 6:06～6:27 | 末张结果完成、全桌亮牌、局末入场；不复制该视频的舞蹈 / 经济系统 |

在现有[验收记录](../acceptance.md#本轮信息减负与特效的执行前记录)填写：实际查看区间、使用的画面证据、原视频出现顺序、准备采用的表现及明确不采用的内容。已有[连续帧](../references/impact-video.md)可帮助定位；原视频或连续画面不可读时先说明缺口，可继续 HUD 信息组织工作，不把文字分析冒充视觉复核。

同一执行任务首次完成上述查看后，后续只复看正在实现的事件片段。未完成对应视觉核对前，不进入该事件的字效 / 3D 修改；不要求每次提交重复通看全片。未实听音轨时，声音具体音色、措辞和听感仍标为未验证。

只借鉴表现：角色与主字分阶段交接、来源与落点、字形厚度、重拍与停留、分席位多响和一次收付。排除牌规、倍率、金额、换牌 / 定缺、直播猫 / 心愿榜和其他产品资产。

### 信息减负先落地，再实现字效

先完成[局内信息层级](../rules.md#局内信息层级)，再给确定的内容制作演出。默认内容及出现时机未收拢时，不继续增加新的文字框、标题标签或光效层。

- `GameTableHud.vue`：简化翻精说明和可胡预览；保留精牌身份、首胡锁手后果及完整合法按钮，保护听牌网格和头像次数。
- `BloodFlowWinCard.vue`：区分决策预览与流水详情，保留完整评分数据；不把流水详情的字段原样铺到局内。
- `BloodFlowWinPresentation.vue`：主番在读字阶段展示一次，收付阶段只留每席正负金额；去掉赢家摘要卡、底框及重复番型 / 倍率 / 胡牌方式，多笔合并只加“合计”。
- 既有流水组件继续提供完整番型、来源和胡杠账本。减少默认字段不删除记录，不改变支付。

预览示例只表达“清一色·碰碰胡 / 预计 +600 · 自摸 · 硬胡”，首次可胡附简短锁手提示。已经发生的结果不再标预计；若本次收益与其他收付合并，明确标“合计”，不能混成一个未说明含义的金额。

### 先恢复公共表现，再完成一个代表性高番

以下代表性高番工作必须在下述“公共表现收拢”完成之后开展。已做过的字形实验保留为证据，不意味着当前可以继续沿血流独立动作、独立飞牌路径扩建。

先在 llmAnime 的单次本家高番上完成可复用标题：笔画与字距、字面 / 描边 / 厚边、透视及冲击，配合现有角色和光柱。起势与阅读可留时间，重拍保持短促；不以整段等比放慢代替运动设计。字体与材质应覆盖全部牌型，不能只把“绿一色”写成特殊分支。

阶段交接以优先视频为准：来源确认 → 角色 / 动作 → 主番重拍与楼位强调 → 稳定阅读 → 分席位收付 → 交回操作。使用已有共享时钟安排，允许必要的短交叠，但主字不能被角色遮挡，不再建立第二套并行队列。

单次演出正常速度与关键帧自检通过后，再覆盖他家胡、本家放枪、两响 / 三响、重复胡及五主题。多响共享一次提示，各赢家分别有主番和收益；主提示可结合来源位置，不要求所有信息都居中。不得为表现一致让未获胜的付款者播放胜利立绘。

## 公共表现收拢：2026-09-06 调整方案

本节是下一轮实施依据，优先于下文第二轮立项时的技术提案。状态只在任务表维护。核对基线为 master `806d1de`；本次制定方案，不表示下面的收拢已经实现。

### 调整理由与保留成果

当前不是所有东西都重写了：`bloodFlowWinEffects.ts` 已调用 `createWinEffectPresenter`，`806d1de` 也已取消正常动态模式下的血流专属光效覆盖。多赢家实例管理、楼位适配和清理本身有必要，可以保留为薄适配。

仍需处理两处分叉：`GameTableHud.vue` 对血流胡类事件返回空，随后 `BloodFlowWinPresentation.vue` 自己组织动作显示，非动漫主题使用简化的 `.blood-flow-action`；`bloodFlowTileFlight.ts` 独立计算飞牌，原 presenter 则关闭 `showWinningTile`。前者的屏蔽条件不包含吃碰杠，不能误报为所有鸣牌都被关闭。

保留：现有规则与账本、同窗合法按钮、普通摸打和副露动画、右端摸牌间隙、三类听牌提示、持续盖楼、共享 cue、主番分席、只显示金额的收付、已修复结算返回。恢复公共实现不等于把血流胡牌接入普通玩法的终局流程。

### 公共层与血流层的职责

| 能力 | 公共层负责 | 血流增量负责 | 本轮处理位置 |
|---|---|---|---|
| 摸打、吃碰杠、选牌、合法按钮 | 原操作栏、动画及阶段契约 | 输出真实合法动作和阶段 | 现有 useBloodFlowGame 适配；无回归失败不重写 |
| 胡 / 自摸动作字与角色 | 现有主题动作模板、AnimeActionCue、角色资源及回退 | 从批次映射赢家、动作类型、席位和播放区间 | GameTableHud.vue、BloodFlowWinPresentation.vue |
| 飞牌、落点光柱、粒子、镜头 | 公共 winEffectPresenter 及从现有实现抽出的必要运动能力 | 来源姿态、目标楼位、记录身份、时钟；多赢家实例适配 | winEffectPresenter.ts、bloodFlowWinEffects.ts、bloodFlowTileFlight.ts、tableTilePresenter.ts、MahjongTable3D.vue |
| 多响、连续胡编排 | 接受外部时间的公共渲染能力 | 同一 cue 的顺序、赢家错峰、重复演出缩减 | 现有 bloodFlow/presentation.ts 与 director |
| 番型标题、楼记录、收付 | 沿用公共字体/主题资源与可用反馈能力 | 主番内容、持续楼位、逐席真实 delta | BloodFlowImpactTitle.vue、BloodFlowWinPresentation.vue、bloodFlowWinPile.ts |
| 声音、模型与气泡 | 原音频/TTS路由、角色、频率设置 | 只提供一次合法事件；胡自由评论禁用、局末感言时机 | 现有 audioBridge / 模型适配，缺口对应 E14 / E15 / G08 |

动作字“胡 / 自摸”和番型字“清一色 / 大三元”职责不同：前者必须复用原模板，后者是允许保留的血流增量。不为统一而删除番型，也不以番型字替代原鸣牌显示。不得要求五主题各自新做一套胡牌渲染器。

### 执行包 1：建立同主题对照与复用边界（E11 / E14 / E16）

1. 实施前重新核对 HEAD 与未提交内容，保留其他任务工作。读当前规则、任务、验收和本节；按第 0 节实际查看优先视频对应连续片段。
2. 同主题、同尺寸分别录制原版和血流的普通胡、自摸、吃碰杠；原版用作基础动作和光效基线，视频用作阶段交接参考。截取原速片段和动作、飞行、落定三个阶段，不能仅比较一张截图。
3. 将入口、资源、时钟、销毁、终局触发逐项列出，标明“已共用 / 必要适配 / 待消除重复”。先覆盖 jade 与 llmAnime 两条动作渲染分支，保留另外三主题原样对照。
4. 冻结当前普通 1900ms、大番 2300ms、顶级 2600ms 作为起始对照参数，不宣称这些就是最终最佳时长。

交付：在现有验收页追加代码基线、看片区间和对照证据。通过条件是明确复用对象及禁改行为，不要求为列清单先修改游戏代码。

### 执行包 2：接回公共动作与飞牌（E11 / E12 / E14）

1. **动作先收拢。** 优先将 HUD 现有主题动作模板原样提取为双方调用的公共显示能力，保留样式、角色选择及回退。普通模式继续原调用；血流按 cue 提供多个赢家及各自进度，不复制模板再分别维护。公共动作显示不负责播放声音。
2. **单一显示入口。** 公共调用接通后，移除血流独立动作字和重复角色装配。将当前“按血流模式屏蔽胡事件”改为明确的事件归属/去重逻辑；不能只删 return null 让两条入口同时播放。每位赢家同一记录只演一次动作，多响不能只剩最后一个赢家。
3. **再收拢飞牌。** 在原 presenter 的运动基础上提供来源姿态、目标姿态、外部开始/落定时间和显示牌交接能力。原调用不传这些参数时保持现有默认效果。血流目标是持续楼位，普通模式目标仍为原胡牌位。
4. 以共享运动能力替换 `sampleBloodFlowFlight` 的独立轨迹计算；必要的楼位查找、历史牌实例管理仍保留。飞行中的牌与常驻楼牌必须只有一个可见拥有者，落稳后交接，不能简单打开 showWinningTile 造成双牌。
5. 自摸取本次摸牌位置，点炮取来源弃牌位置，抢杠取对应补杠位置；一炮多响允许各赢家有展示牌，但实体来源和支付仍只按规则处理。恢复历史直接摆最终楼位，不重播。
6. 保持 `806d1de` 的原光柱/粒子默认效果及减少动态效果设置。适配器可以继续管理多个公共实例、释放资源和选取单次镜头反馈，不另开队列、不累计三次强震。

交付：动作与飞牌可分两次独立提交，每次公共调用接通并回归后才移除旧分支。通过条件：原版外观/动作无回退，血流无双字、双立绘、双牌、瞬移或缺失光柱，局内胡牌不触发亮全手和结算。

### 执行包 3：节奏和信息交接（E11 / E13 / G02～G05）

使用已有 director 的唯一时钟安排：来源确认 → 原动作/获胜角色 → 飞牌落楼与主番重拍 → 稳定读番 → 一次收付 → 开放下一操作。动作可以有短交叠，但角色不能遮主字，旧结果不能覆盖下一轮操作预览。

- 普通摸打与吃碰杠继续原阶段和报牌等待；本轮不把所有动作统一延迟，也不以整段等比减速制造可玩性。
- 分别测量“牌可辨认的时间、主番稳定可读时间、下一操作开放时间”。在当前时长基线上调整阶段占比；需要调整总时长时记录原速前后片段及原因。主番阅读先试约 500～800ms，属于试调值，不能直接覆盖普通动作时长。
- 锁定/禁操作仅覆盖必要的当前演出及摸牌阶段；进入有效响应后显示全部合法候选，不能为了降低密度藏碰/杠或强制先过。启用倒计时的实例从可操作时开始计时，保持默认大厅无倒计时配置。
- 日常仅保留牌桌、局况、头像积分/次数、精牌身份和必要提示；详细规则与账本主动打开。动作阶段不显示赢家摘要卡；主番只说一次；收付每席一次正负数，多笔合并才加合计。
- 明确分开“上一事件已发生收付”和“当前预计收益”。不能用继续增加标签解决两个事件同时占主区域；优先错开出现时段。
- 连续胡缩短重复的装饰入场，保留来源、动作身份、落牌及金额。不得把 AI 推进或整局 autoplay 当作节奏修复。

交付：等待、选牌、可胡、演出、收付五状态原速片段。通过条件：不暂停录像也能辨认谁出牌、谁响应、牌去哪里、现在能做什么；自动化只证明时序边界，人工观看验证可读性。

### 执行包 4：高番、多响和五主题复核（E11～E14）

先复用当前番型组件，在 llmAnime 单次本家高番验证“原动作 → 主番”，再扩展其余主题。若公共动作已经解决主要违和，不先推翻全部番型字形。需修改字效时再复看优先视频 1:16.5～1:19.8。

- 番型增量只调整必要的字面、短厚边、过冲/回弹、稳定阅读及席位位置；避免软糖般圆厚浮雕、细笔画多重描边、持续晃动。五主题沿用自身已有视觉语言，不照搬视频金字、不从地名造符号。
- 普通胡保留原基础冲击；高番增加短促标题重拍和可读停留。不能通过削弱普通胡光柱来制造等级差异，也不能让每次普通胡都全屏压暗。
- 点炮胡突出来源与赢家；本家放枪不播放本家胜利角色。多响一个来源提示，各赢家各自主番，统一金额阶段；付款者显示总支出一次。分席同播或轻错峰服从可读性，不强制全部挤到中心。
- 重复胡按席位与主番处理，另一家首次高番不能被全桌冷却吞掉。跨层落牌保留，无层数截断。
- 依次检查 jade、rosewood、happyMahjong、llm、llmAnime；llmAnime 检查真实获胜角色，不能用桌布人物代替。先 1280×720，再 844×390 与 568×320；小屏优先调整锚点和舞台占用，不隐藏合法按钮。

交付：同一场景五主题对照、单次高番原速及多响连续帧。通过条件：字清楚、主次分明、原基础效果可见、来源与席位正确；用户未认可的外观明确标为待确认，不能用截图生成成功代替认可。

### 执行包 5：声音、真实对局与结算闭环（E10 / E14～E16 / G07～G08）

1. 原动作人声继续现有资源/固定 TTS/配置路由；配置 LLM 不等于全部声音改走新 TTS。非人声胡牌效果音复用既有资源，在共享重拍播放一次；测试静音、重复事件、迟到语音和离场取消。
2. 普通 LLM 摸打/吃碰杠气泡按原主题、性格与频率；强制动作不为聊天额外请求。局内胡牌不生成自由评价，只有 llm / llmAnime 在本局结束后有感言。模型/音频失败不能锁住操作。
3. 正常大厅开始，人工正常速度操作至少完整一局，直到局末结算，再查牌桌、返回结算、进入下一局。若由用户操作、助手旁观，必须如实记录；高速自动局不能替代。
4. 最终排名路径用场末输入补验：最终排名 → 最后一局 → 明细 → 返回；×/查看牌桌 → 牌桌 → 返回结算；同局刷新不重弹，退大厅重开无旧桌残留。
5. 自然未触发的补杠/抢杠、两响/三响、高番、长楼和末张，用现有固定输入补验并明确来源。真人实听与带音轨证据独立记录；无实听能力时保持待验收，不声称声音通过。

验收优先复用现有用例：`blood-flow.claim-actions`、`common-hand-gap`、`playability`、`flights`、`effects`、`payments`、`refinement-layout`、`audio`、`llm`、`reactions`、`settlement-navigation`。调整公共 renderer 必须同时检查非血流；补测行为边界而非只测组件名或函数调用次数。每包跑相关用例和类型检查，最终再跑前端全量测试；不要求每个文档修改或微调重复全量测试。

最终验收表分开记录：代码已提交、自动化结果、固定输入视觉、完整实玩、声音实听、用户视觉认可。附提交、主题、尺寸、步骤、证据及未验证项，不继承旧版本的整体通过结论。

### 不纳入本轮的事项与交接

- vibehub 迁移/同步、真实 P2P、后端、性能与安全工程继续按用户当前范围后置。
- 首胡后自动胡/自动摸切仍是候选交互，视频不能证明具体开关与过胡语义。本轮保留现有手动控制及锁手规则；以后单独确定适用动作、取消/过胡与控制权，再实现，不能夹在演出重构中偷偷启用。
- 一个实施任务顺序推进上述五包。确需多任务时，前包完成并记录基线再交接；不得同时改 HUD、BloodFlowWinPresentation、公共 presenter。
- 每包交付说明“调用回了哪项公共能力、移除了哪段重复实现、保留哪些血流增量、原版如何回归”；未完成公共收拢前不以扩展主题美术代替修复。

## 1. 这轮只补游戏表现

- 范围：结算窗口关系与按钮、最终排名、本局结算、胡牌字效、飞牌落楼、多响、正负分反馈、五主题以及局末感言的呈现位置。
- 不调整玩法、番型分值、硬胡、底分、封顶、首胡锁手或 AI 决策策略；不纳入性能优化、安全审计、后端开发或新的联机架构。
- 保留现有两骰 / 翻精 / 发牌；保留已经恢复的 3D 光束、粒子和原胡牌位。
- 保留持续向上盖楼，不恢复旧方案的三层 / 两层截断。
- 复用现有胡 / 自摸 / 吃碰杠报声代码、资源和角色音色；不新增替代报声、不双播。
- 局内胡牌无自由感言；正常摸打、吃碰杠的已有 LLM 动作短句与气泡应保留，不能一并关闭。只有 llm / llmAnime 在本局结束后允许全桌感言；其他三个主题无角色感言。
- 用户所说“结算混乱”的直接反馈是按钮返回不通，不把此前关于分数理解的推测当成新的用户需求。结算外观补齐必须保持已修复的操作路径。
- 不从“莲花”地名推导花卉、地域装饰或世界观。参考视频的动作节奏，不复制其金额、充值流程、角色或完整 UI。

### 1.1 本轮补充：立绘、声音、动作气泡

用户询问 `llmAnine`，本方案按现有主题 ID `llmAnime` 理解。

- `llmAnime` 的胡牌演出包含获胜席位的角色立绘，立绘与字、光、飞牌、数字共享同一 cue。优先复用 `AnimeActionCue.vue` 的角色选择、动作图与回退资源，沿用已确认的立绘尺寸和席位锚点；不把桌布固定人物当成玩家。
- 本轮明确接入非人声胡牌效果音，优先复用已有 `public/audio/hu_effect_sound.mp3`，与落牌 / 标题重拍同步。它与原“胡 / 自摸”等人声报声是两个通道，不替换或重复原报声。
- E14 立项时已有 `audioBridge.ts` 人声接线、缺非人声效果音；实现及实听状态分别见任务表和验收记录。用户报告“听不到胡牌音效”时，两类声音分别验收；不能因代码调用或 spy 通过就认定实际可听。
- E15 立项时 `bloodFlowRuntime.ts` 要求 `message` 为空且忽略所有自由文本，`GameTableHud.vue` 又只显示局末 `roundBubbles`，因此普通动作气泡也被禁止。这是应收窄的实现范围，不是用户要求所有局内 LLM 都沉默。
- LLM 相关主题、已启用 LLM 玩家时，正常决策中的摸打 / 吃碰杠短句沿用原角色风格和频率。摸牌本身不为发言另发一次模型请求；无需模型选择的强制动作也不为聊天额外请求。
- 胡牌仍只走现有动作报声、角色立绘和视觉提示，不接受胡牌评价或付款者反应；整局结束后才是全桌感言。

| 内容 | llm / llmAnime | 其他三个主题 |
|---|---|---|
| 现有胡 / 自摸 / 吃碰杠报声 | 沿用原路由 | 沿用原路由 |
| 本轮非人声胡牌效果音 | 接入，服从静音与现有音效设置 | 接入，服从静音与现有音效设置 |
| 普通 LLM 动作短句气泡 | 启用 LLM 玩家后按原策略显示 | 不新增角色聊天气泡 |
| 胡牌自由评价 / 付款者反应 | 不触发 | 不触发 |
| 全桌局末感言 | 本局结束后允许 | 无 |
| 胡牌角色立绘 | llmAnime 接入；llm 不套二次元角色演出 | 沿用各主题自身视觉 |

## 2. 对比结论到任务的对应关系

| 立项时发现 | 本轮落地 | 验收中必须看见的结果 |
|---|---|---|
| 关闭结算后没有明确返回，快照刷新会重新弹窗 | E10 保留并扩大回归范围 | 查看牌桌 / × 收起窗口，返回结算可用，同局刷新不抢回窗口 |
| 血流缺独立最终排名，末局只显示“本场已完成” | E10 | 场末显示四家最终名次、头像和累计总分 |
| 结算页主要是表格和完整流水，与旧版主题结算差距大 | E10、E15 | 本局结果有清楚主视觉，明细按需打开；五主题保持自身风格 |
| 楼里新牌直接出现在最终位置，旧光效未包含飞牌 | E11、E12 | 来源 → 目标层的运动、轻弹落稳与原位光效组成一个过程 |
| DOM 与 3D 各推进队列，400ms / 750ms 等时长不同 | E11 | 字、牌、光、数字属于同一批次且按共同节点出现 |
| 多响只在中央展示最强一家番型 | E13 | 中央说明几响，各赢家分别显示自己的主番与来源 |
| 只对赢家显示正分，付款及杠分反馈弱 | E13 | 四家真实正负变化可见，杠分复用既有飘分表现 |
| 四档主要改变字框、阴影与时长，3D 差异小 | E14 | 普通 / 中番 / 大番 / 顶级的运动、构图和爆发强度有区别 |
| 完整演出冷却全桌共享，另一家首次大番被压缩 | E11、E14 | 同席同主番重复被压缩，其他席位首次大番仍能正常表现 |
| 血流旧子特效产生的镜头 / 亮度反馈未被主渲染器采用 | E14 | 高番可选择轻强调，多响不会累加成三次强震 |
| 局末感言在牌桌气泡里，自动结算窗口会遮住 | E15 | 允许感言的主题在结算人物区展示，不需要关窗口找气泡 |

## 3. 实施顺序与提交边界

E10 → E11 → E12 → E13 → E14 → E15 → E16 是第二轮初始依赖；后续复修改按当前任务表的五个执行包推进。每包在 master 独立完成并记录证据，保留其他任务改动。本阶段用户已将 vibehub 同步与 P2P 验证后置，先完成前端单机体验。

阶段依赖与当前状态统一维护在[任务表](../tasks.md)。

## 4. E10：结算宿主与按钮路径

现有文件：`src/components/table/GameTableHud.vue`、`src/components/settlement/BloodFlowRoundLedger.vue`。拟新增：`BloodFlowSettlementHost.vue`、`BloodFlowRoundSummary.vue`、`BloodFlowFinalRanking.vue`。优先抽取旧结算可复用的布局 / 主题样式，不整文件替换原 `SettlementOverlay.vue`。

- **E10-1** 用一个宿主管理 `table / round / final / details` 四种展示状态和明细返回目标。只能有一个可交互结算层，不叠两个遮罩。
- **E10-2** 保留 fc86da7 的按局 ID 自动打开一次。× 和“查看牌桌”均回到 table，桌面保留“返回结算”；明细另有明确的“返回结算 / 返回牌桌”按钮，不依赖用户猜 × 的含义。
- **E10-3** 非末局打开本局结果，末局打开最终排名；最后一局结果可从最终页主动查看，不能自动连续弹两页。
- **E10-4** 本局页沿用既有头像、昵称、本家标识、正负分和当前积分的展示语言；胡牌次数、胡牌收支、杠牌收支保留为辅助信息和详情，不删除账本信息。
- **E10-5** 最终页按权威累计总分 / 排名展示四家，不能把最后一局 delta 当整场分数。没有完整场次历史时不伪造“全场逐局回放”。
- **E10-6** “继续下一局”只推进一次，并接入既有权威准备状态显示等待；“返回大厅”只离场。失败时保留可恢复路径，不能因本机按钮标记永远锁住。不要用纯计时器假装其他玩家已准备。

通过条件：普通局、末局、无胡局、有席位筛选的流水均能“结算 → 牌桌 → 结算”；按 × 后同局新对象不重弹；进入下一局清理旧入口；P2P 等待时仍可查看牌桌并返回。原 `blood-flow.settlement-navigation.spec.ts` 全部保留。

提交建议：`feat: complete blood-flow settlement views and navigation`。

## 5. E11：统一胡牌表现调度

现有文件：`src/game/variants/lotus/bloodFlow/presentation.ts`、`BloodFlowWinPresentation.vue`、`three/bloodFlowWinEffects.ts`、`GameTableHud.vue`、`MahjongTable3D.vue`。拟新增：共享 `presentationDirector.ts` 及只读 cue 类型。

- **E11-1** 每个观看端只保留一个调度器创建 cue，DOM 与 3D 消费同一 `cueId / batchIds / startedAt / duration / phaseMarks`，不各自 next 一次队列。
- **E11-2** cue 包含绝对来源席位、动作种类、赢家逐席番型摘要、逐席收付向量和展示记录 ID。局部坐标在观看端转换，牌实体账不读取 cue。
- **E11-3** 明确 `focus / impact / readable / score / exit` 时间节点。先统一节拍，再调字形与粒子，避免不同模块各自写 setTimeout。
- **E11-4** 同席同主番记录完整演出冷却；更高番或另一家首次大番可完整出现。中央舞台仍排队，不能同时塞多个中央标题。
- **E11-5** 连续批次积压时，合并 cue 的收付必须是所有包含批次的实际合计；若显示最后一笔金额，必须明确标注最后一笔，不能用“多次胡牌”标题配未说明的单笔数字。
- **E11-6** 恢复 / 重连只构建最终展示态，不重播历史 cue。单机阶段、下一动作开放及倒计时共用明确的时间安排；旧 450ms 默认值已由可玩性修复调整，不作为不可变约束。装饰动画 / TTS 回调不直接提交规则动作；新动作开放后，收尾画面不能遮操作区域。
- **E11-7** `llmAnime` 增加受 cue 控制的角色层：绑定获胜席位 / 角色 ID / 胡牌动作种类；角色入场、主字重拍、飞牌落点与退场用同一时间安排。不能只取消旧胡牌立绘屏蔽，让旧 CSS 时间线与新演出双播。

通过条件：给定同一时间输入，角色立绘、文字、光效、飞牌和数字对应同一批次 / 记录；连续十胡及中途新批次到达不串台；多响各角色对应正确玩家；普通吃碰杠立绘照常工作。调度器不能调用计分、摸牌或再次触发现有动作报声出口。

提交建议：`refactor: share blood-flow presentation timing across renderers`。

## 6. E12：胡牌张飞入楼层

现有文件：`three/tableTilePresenter.ts`、`three/bloodFlowWinPile.ts`、`three/bloodFlowWinEffects.ts`、`three/winEffectPresenter.ts`。拟新增独立的新记录落层 tween，不改变旧模式默认演出。

- **E12-1** 在公开来源动作更新前保存可用的展示变换：自摸取本次摸牌位、点炮取弃牌位、抢杠取杠位。来源布局不可用时从该席位的合理牌区起飞，不从错误玩家或固定中央冒出。
- **E12-2** 新记录从来源飞向它自己的 `column / level`，到达后短回弹再稳定。目标始终为已有胡牌位和持续增高的对应层。
- **E12-3** 飞行动画暂时接管该记录的可见牌，常驻楼牌实例暂不重复绘制；完成、跳过或恢复后交还常驻实例。这个接管只影响显示，不操作牌库。
- **E12-4** 一炮三响可有三条展示飞行引用，但共用唯一来源事实；三张展示牌最终分别落到正确赢家楼层。
- **E12-5** 第 13、25、41 次胡等跨层边界都有正确落点；不重放已有楼牌，不改回截断或收纳。

通过条件：慢放能看见“从哪里来—落到哪一层—落稳”，每个记录只有一个可见落牌结果；新记录和历史恢复行为明确不同。

提交建议：`feat: animate blood-flow winning tiles into persistent towers`。

## 7. E13：多响与四家收付反馈

现有文件：`BloodFlowWinPresentation.vue`、`BloodFlowWinCard.vue`、`GameTableHud.vue`、既有 `PlayerSeat.vue` / 本家飘分通道；根据需要增加只读公开收付表现数据，不修改计分公式。

- **E13-1** 多响只出现一次“二响 / 三响”公共提示，可结合来源位置。读字阶段各赢家显示自己的主番，不能把最强一家番型套给全批次；收付阶段只显示各自金额，不再重复番型、倍率或胡牌方式。
- **E13-2** 从 batch.deltas 产生四家正负分。付款者显示本批总支出，赢家各自显示实际所得；自摸三付、点炮多响、抢杠均清楚区分来源。
- **E13-3** 支持赢家同时有另一笔支出的合并情形；多笔合并数字标“合计”，不把正分、负分或零净值误当成丢失记录。
- **E13-4** 杠分复用已有正负飘分样式。如果当前座位视图只提供胡牌 batches，就从既有权威杠账本增加只读、有 ID 的收付事件；不凭两次快照总分差猜测某一笔杠分，也不改其他任务正在调整的 AI 决策。
- **E13-5** 正负数字在 cue 的收付阶段出现，当前权威积分照常更新，不等动画才扣分。每席同笔变化只显示一次；不加赢家摘要卡或重复主番/倍率/动作，身份由头像、位置及无障碍标签承担。完整评分数据保留在流水。

通过条件：使用三个不同番型的一炮三响，一眼能读出每家胡什么、谁出牌、四家各变多少；总支出与各收入一致；杠分可以看见而不是总分无提示跳变。

提交建议：`feat: show per-winner patterns and four-seat score feedback`。

## 8. E14：四档打击感和五主题

现有文件：共享 cue、`BloodFlowWinPresentation.vue`、3D 血流 presenter、`MahjongTable3D.vue` 与 `src/theme/` 的主题配置。对旧 `winEffectPresenter` 的扩展使用可选参数，旧模式默认表现不变。

- **E14-1** 标题改为短冲入、过冲、回弹、读字、收势，替换单纯 0.8 → 1 渐显。主番在视觉上是主体，修饰和公式不同时抢主标题。
- **E14-2** 按2026-09-06用户要求，五主题的普通胡、高番和多响均恢复对应非血流版本的原光束、光晕、星芒和粒子。分档继续通过字效、构图和共享时序表现，不再覆盖原光束颜色、宽高或强度；保留可见落点、收付及减少动态效果设置。
- **E14-3** 本家大番使用主舞台，他家使用席位舞台；多响配合 E13。重复胡压缩停留，但保留落点重拍。
- **E14-4** 大番选择性使用背景压暗、落点闪光及轻微镜头强调；将子特效的画面反馈统一交给渲染器。多响取受控主反馈，不把三份镜头震动叠加。
- **E14-5** 五主题分别调字形、配色、边缘、轨迹及材质，不只换边框颜色。墨玉、红木、欢乐、模型、二次元仍保持各自概念；不生成地名衍生图案。
- **E14-6** 原动作报声保持现有调用和音频不变；明确接入已有非人声胡牌效果音 `hu_effect_sound.mp3`，对齐 cue 重拍并独立去重。多响共享一次主重拍，重复胡可用紧凑强度；不追加“我胡了”，不把气泡或模型感言当打击音。
- **E14-7** 分开验收动作人声与胡牌效果音：检查自摸 / 点炮 / 抢杠、普通 / 顶级 / 多响、静音开关、角色 TTS 可用及不可用。确认音频实际播放、非零音量和可听结果；原报声缺失时修复接线 / 回退，不以增加一份新录音掩盖问题。

早期 0～100ms 聚焦、100～220ms 飞牌等数字只是历史首测提案，已不足以指导当前可玩性与字效。以后先分清准备、冲击、阅读与收付的职责，再依据优先视频的阶段关系和本机正常速度实测调整；不直接复制视频内的倒计时，也不恢复旧瞬间飞牌。

通过条件：同一事件在普通与顶级两档有明确差异；不读倍数也能辨别层次，但能够读清番型；主次、方向和落点明显；原动作报声未被替换或双播。

提交建议：`feat: strengthen themed blood-flow win impact`。

## 9. E15：局末过渡与感言落点

现有文件：E10 结算宿主、`bloodFlow/useBloodFlowGame.ts` 的结果表现接线、现有 `roundBubbles` / 局末反应通道；恢复普通动作气泡还涉及 `game/llm/bloodFlowRuntime.ts`、既有 `decisionSpeech.ts` / `speechPolicy.ts`、`GameTableHud.vue` 与 P2P 动作消息接线。只改发言准入和展示，不改正在进行的 AI 选牌 / 胡牌策略，也不恢复局内胡牌感言。

- **E15-1** 明确最后一次胡牌收势、本局结束亮牌、结算入场的关系；结算窗口不能抢在最后一次结果完全不可读时盖住一切，也不等待全部云端语音才允许操作。
- **E15-2** llm / llmAnime 的局末感言显示在对应结算玩家卡；头像、发言者及身份保持一致。其他三主题无感言。
- **E15-3** 查看牌桌时可继续在已有席位区显示同一份局末文字；返回结算不能重新生成或播放一次。两个展示位置不能同时发起 TTS。
- **E15-4** 下一局、返回大厅或主题变化时取消旧感言表现；不影响原动作音和当前合法打牌决策。
- **E15-5** 结算关闭 / 重开使用恢复态，不重新播放全套入场；所有按钮路径继续通过 E10 回归。
- **E15-6** 收窄血流模型的全局禁言：正常决策可返回短 `message`，胡牌动作仍禁自由感言。消息经既有短句 / 事实过滤，并在权威确认该动作实际执行后显示；被胡优先级打断的碰 / 杠不能提前宣称已执行。
- **E15-7** 普通 `actionBubbles` 与局末 `roundBubbles` 分开准入；普通动作气泡沿用既有性格、频率和用户设置，不等待局末才显示。胡牌到来不生成新评价。P2P 房主只生成一次动作短句，客机显示对应已执行事件，不重复请求模型。
- **E15-8** 有原动作报声的吃碰杠允许显示匹配气泡，但沿用原语音出口，不能将同一句动作另走 TTS 再播一遍。若启用普通决策语音，也沿用既有设置和避免双播的策略；不为每次摸牌另发聊天请求。

通过条件：正常摸打 / 吃碰杠的合法 LLM 气泡能按原设置出现；胡牌自由感言请求与展示仍为零；无需关闭结算寻找局末说话者；声音失败或静音时按钮照常可用；固定动作语音不双播。

提交建议：`feat: align blood-flow round transitions and end reactions`。

## 10. E16：对照验收与同步

- **E16-1** 保留一套固定演出输入：普通自摸、硬胡清一色、十三幺封顶、点炮、抢杠、三种番型三响、同席十连胡、另一席首次大番、跨层盖楼、末张胡。
- **E16-2** 在同一主题和视口录制修改前 / 修改后短片，检查重拍、牌的方向、字的可读性、四家收付与下一动作。测试中“存在一个特效节点”不能代替视觉验收。
- **E16-3** 按五主题检查本局结算、最终排名、明细、牌桌四种状态及按钮返回；桌面 1280×720 与小横屏 568×320 / 844×390 必须覆盖。
- **E16-4** 执行相应 Vue / presenter / 布局单测和浏览器用例，再跑类型检查、完整前端测试与构建。旧两模式的结算和原动作报声必须回归。
- **E16-5** 当前已修复的 `blood-flow.settlement-navigation.spec.ts` 与原布局、特效用例保留；增补同源 cue、飞行交接、各家多响标题、正负飘分、感言位置用例。
- **E16-6** 每批共享改动在 master 提交后执行规定同步，保持现有 P2P 壳层。遇到其他任务未提交改动时不覆盖 / 不擅自 stash；同步完成后在 P2P 工作树验证共享页面和按钮。
- **E16-7** 更新验收记录，逐项对应第 2 节差距；哪些已修复、哪些只是提案、哪些尚未验证必须分开写。未获发布授权不部署。
- **E16-8** 补充带音轨的实际体验样片及立绘 / 气泡矩阵：llmAnime 角色先后时序正确，现有报声与非人声重拍分别可听，普通动作气泡存在，胡牌自由评价不存在，局末感言只在两个 LLM 主题出现。自动调用断言不能代替实听；无实听条件时明确标为未完成。

## 11. 交付与完成标准

本轮交付不以“再加一个特效”计算完成。应同时满足：

- 结算按钮关系明确，原返回修复没有回退；本局结果与最终排名各有正确主状态。
- 一个胡牌批次有统一的文字、3D、飞牌、落楼及收付节点。
- 多响每家有自己的番型与金额，付款者反馈完整。
- 新牌确实落到持续增长的楼层，已有牌不会重复飞入。
- 高番有更强表现，本家 / 他家 / 重复胡有主次。
- 局末感言在可见的玩家区域，严格沿用主题与时机限制。
- 原玩法、原动作报声、牌规与积分没有被本轮表现调整改变。

实现提交和测试证据统一记入[验收记录](../acceptance.md)。
