# 血流验收记录与复测入口

更新日期：2026-09-06。本页维护已有证据和待取得的证据；最新补修为五主题恢复非血流原胡牌光效。早先“仅整理文档”的说明只适用于页末那次整理。任务进度只维护在[当前任务](tasks.md)。

## 2026-09-06：全部五主题恢复非血流原光效

基于master `baee4f0`，按用户明确的“每一个主题”实施。血流原本复用 `createWinEffectPresenter`，但额外传入 `bloodFlowImpactProfile` 覆盖了颜色、光束宽高、光晕、星芒和粒子。现正常动态模式取消这层覆盖，五个主题直接使用非血流同一渲染器的原有默认值；仅既有减少动态效果分支保留辅助覆盖。没有改原渲染器、飞牌落楼、字形、共享时间节点、收付金额或声音路由。

五个主题均实际抓取并查看：非血流 `/?theme=…&winEffectLab=1` 原胡牌光束，以及血流固定输入的普通胡、高番、三响。首轮原版采样部分仅覆盖动作入场，已补采2.8秒起的光束阶段，最终对照使用 `*-original-beam-*.png`。1280×720证据：[左原版、右血流恢复后](../../work/blood-flow-original-light/five-theme-compare.jpg)、[五主题高番与三响](../../work/blood-flow-original-light/five-theme-high-multi.jpg)。内芯、外层渐隐光晕、底部星芒及散射粒子已逐主题目视核对；血流仍落在各赢家的实际楼位，因此不把两种玩法的整桌布局差异当成光效差异。

类型检查通过；`blood-flow.effects.spec.ts` 与 `blood-flow.flights.spec.ts` **14/14通过**，含五主题、三响、重复事件、资源失败恢复及13/25/41次跨层落牌。此次为指定光效恢复与固定输入对照，未重跑完整对局、小屏或声音实听，未同步vibehub或执行联机迁移。取证文件在本机忽略的 `work/blood-flow-original-light/`，未上传或随Git提交；其他任务的参考文档改动保留。

## 2026-09-06：收付阶段只留金额

基于 master `002712b`。用户指出此前“平胡 / 1倍·点炮胡 +10”仍在重复前面已经展示过的动作和主番；旧两行摘要方案不再作为现行展示要求。现取消赢家摘要卡及胡牌收付底框，收付阶段只显示每席一次正负金额，多笔合并才加“合计”。单笔不再显示主番、倍率、硬胡或胡牌方式；前面的逐席主番、动作/角色、时序、光柱/飞牌及完整流水数据保持原实现。杠收付沿用原入口。

已目视核对 [1280×720普通点炮](../../work/blood-flow-refinement/amount-only-1280.png)、[568×320](../../work/blood-flow-refinement/amount-only-568.png)，本家仅 `+10`，付款者仅 `-10`；[三响收付](../../test-results/blood-flow-different-patterns-three-win.png) 只显示三家 `+80/+160/+320` 和付款者 `-560`。这些是固定输入补验，不声称新完成一局实玩。上一轮真实对局及旧截图保留作历史记录。

类型检查通过；收付/预览/合并零净值/十连胡定向浏览器5/5通过，llmAnime桌面1280×720与小屏568×320布局2/2通过。首次命令因前端服务未运行而连接失败，单独启动4175前端后重跑通过，没有启动后端或联机服务。本次不重跑整局或声音验收，不将此前全量检查算成本次重跑。

## 本轮信息减负与特效的执行前记录

本轮开始核对：master `0a76c91`，工作区干净；任务安排 `94b78ee` 后的 TTS 缓存修复保留。只做前端单机，vibehub / P2P / 后端均不执行。执行顺序见[任务表](tasks.md)。

### 执行包 1：本轮实际看片（2026-09-05）

Codex 已用图像查看工具实际查看源视频对应的四张全片概览（0:00～6:24，每 6 秒），随后查看下列连续帧，并非仅阅读旧分析。源文件 `D:/Documents/Downloads/843049425-1-208.mp4` 可读，133156944 字节。复用已有原片抽帧，未移动或上传视频；证据目录 `work/video-reference-843049425/` 为本机忽略产物。未逐帧查看全片，**未实听音轨**。

| 实际查看区间 / 画面 | 本轮观察到的顺序 | 采用的手法 |
|---|---|---|
| 1:04～1:10，`discard-win.jpg` + 本轮从原片新抽 `recheck-discard-tail.jpg` | 对家供牌 → 胡/过 → 本家角色与来源光柱 → 角色退下、落楼与三连刻 → 两家一正一负 → 1:10 续行 | 来源确认、角色与主番交接、一次收付 |
| 1:16.5～1:19.8，`title-impact.jpg`，0.3 秒连续采样 | 自摸角色 → 收角色与手部落牌 → 四暗刻超尺寸斜透视字 → 短闪光、缩回读稳 → 三负一正 | 字面明暗、暗厚边、逐字大小/倾斜差、短重拍与稳定停留 |
| 1:59～2:03.5，`self-draw.jpg`，0.5 秒采样 | 新摸牌与胡按钮 → 自摸角色 → 角色退出 → 飞牌/落楼光柱与四暗刻 → 收付 → 续行 | 动作先于番型，数字晚于主番 |
| 5:44.5～5:48.5，`discard-loss.jpg` + `multi-win-late.jpg` | 本家打牌 → 左家胡角色、来源竖光 → 左家主番/楼位 → 本家负数、左家正数 | 付款者与赢家身份分开，付款者不播胜利立绘 |
| 5:49.5～5:54，`multi-win-late.jpg` + `multi-summary.jpg` | 右家供牌 → 来源侧一次一炮多响 → 对家/本家分席角色 → 分席断幺九/三连刻 → 右家总支出和两家各自收入 | 公共提示先行，所有赢家的番型各自可读，最后统一收付 |
| 6:06～6:27，`ending.jpg`，2～3 秒采样 | 末张胡按钮 → 最后大胡 → 全桌亮牌 → 对局结束 → 角色结算 | 最后一笔收完再亮牌和结算，保留返回路径 |

明确排除参考中的番型规则、金额、换牌/定缺、直播猫、心愿榜、角色及其他产品资产、舞蹈和经济系统。五主题沿用各自概念，不从地名或牌型名称派生花卉/地域/世界观。

进入字效 / 3D 实现前记录：

- 实际查看的原视频区间：点炮胡、自摸、高番字效、本家放枪、多响、局末。
- 查看方式和证据：原片播放 / 慢放 / 连续帧；未实听音轨则明确填写未实听。
- 每种事件的观察：来源何时强调、角色何时出现和退出、主字何时重拍并可读、收付何时出现。
- 拟采用与不采用的内容：采用表现顺序和视觉层次，不移植牌规、金额、直播覆盖物和产品资产。

此前参考分析已准备好片段，但新执行者仍需实际复看对应画面，不能把“已读取分析文档”填写成“已观看视频”。

### 各执行包的验收记录

#### 执行包 2：默认信息减负

基于 `ff0658a` 实施。1280×720、llmAnime 固定展示输入已逐张目视核对 [等待](../../work/blood-flow-refinement/info-waiting.png)、[选牌](../../work/blood-flow-refinement/info-selection.png)、[可胡](../../work/blood-flow-refinement/info-preview.png)、[收付](../../work/blood-flow-refinement/info-payment.png)。等待仅留精牌身份；选牌仍为原四列牌面/倍率/余张网格；可胡明确“预计 +600”，胡/碰/杠/吃/过同时可见；收付为赢家“绿一色 / 64倍·硬胡·自摸 +1920”两行和三家各一次 -640，没有第二笔赢家浮分。fixture 顶部控制条和收付时仍留的操作按钮是固定输入控制状态，不冒充正常引擎的操作开放证据。

二骰与替代说明默认折叠，点击精牌可查；预计详情保留全部番型、公式、排除项及支付数量。首次可胡简短说明胡后锁手，后续短状态；演出期间不展示下一笔预览。流水组件及数据未删除，听牌网格/头像次数/同窗按钮没有重做。`blood-flow.refinement.spec.ts` 四状态用例 1/1、类型检查通过；正常速度动态和小屏留给后续包，不以本次静态截图声明通过。

实施期间发现另一任务正在修改 `useBloodFlowGame.ts`、血流类型、评分 worker/service 及 `handWaits.ts`；未覆盖、暂存或提交其改动。本轮只提交所属 HUD/演出/展示夹具及本节记录。

#### 执行包 3：本家高番代表

基于 `023cb52`，先完成 llmAnime 本家绿一色。新增通用 SVG 字面组件，逐字轮廓与字距、暗侧挤出、薄亮边、渐变字面分开绘制；五主题各有字形/材质，不全套金字。同一 cue 的 880ms 重拍、1100ms 回弹读稳、1750ms 收付；总时长仍为顶级 2600ms/大番 2300ms，准备更完整而冲击约 130ms，未等比拖慢整段。原光柱/粒子与飞牌仍消费共享时钟，普通胡保持 1900ms。

正常速度浏览器触发并观察角色与飞牌，另录制 [原速 WebM](../../work/blood-flow-refinement/single-high-normal.webm)，包含高番和普通点炮补验，**无录制音轨、未实听**。逐张核对 `single-high-000～017.png` 与实际采样时间 `single-high-times.json`；[角色](../../work/blood-flow-refinement/single-high-001.png)、[主番读稳](../../work/blood-flow-refinement/single-high-004.png)、[普通光柱与粒子](../../work/blood-flow-refinement/ordinary-baseline.png) 分别可见。首轮把两字平胡铺满标题宽度导致普通字过大，已按字数及等级设宽，复录后普通与高番层次分明。角色结束后主字才进入，稳定主字在桌心方位上方，手牌/操作区可见。

代表用例 1/1、标题/飞牌定向单测 2/2、类型检查通过。**用户随后指出本家绿一色被放到对家、平胡仍靠桌心，位置验收不通过；撤回此前本段位置通过结论。** 原截图保留为 `rejected-high-position.png` / `rejected-ordinary-position.png`，不得作为正确席位样本。修正原则：所有档位按赢家席位定位，本家统一在手牌前方，高番仅改变大小和运动，不改变归属。重录与复核结果继续记在包4。

#### 执行包 4：纠正本家位置，扩展放枪/多响

本家全部番型统一锚定手牌前方，取消高番移到上方的分支；普通/高番只改变尺寸和冲击。重新录制并目视核对 `single-high-004.png`、`ordinary-baseline.png`，绿一色与平胡均归本家前方，桌心保留。固定展示夹具同时补上此前遗漏的 `data-table-theme`，使头像、按钮和手牌样式与正常应用一致；本节新帧/视频覆盖旧同名产物。

已实际查看本家点炮胡、本家放枪、他家自摸、二响、三响的 `*-source / *-actors / *-titles / *-payment.png`；[原速连续录像](../../work/blood-flow-refinement/source-winners-normal.webm)。同一来源只一份提示，三响角色只在右/对/左赢家出现，本家付款者没有胜利角色。角色退场后，右家清一色、对家大三元、左家十三幺分别读字；[收付](../../work/blood-flow-refinement/three-winners-payment.png) 对应 +80/+160/+320，本家只一次 -560。二响则本家清一色/对家大三元，按各自席位读取；没有把最强番型套给所有赢家。

十连胡补验：首笔单独演出，积压九笔合并为实际净收付，本家“最近：绿一色 / …合计 +17280”，三家各 -5760；[画面](../../work/blood-flow-refinement/ten-wins-merged.png)。胡次数从3到13，重复投影不再增加、不重放；原持续盖楼、飞牌接管和去重机制沿用。流水数据没有因合并而删除。

本轮四状态、代表高番、五类来源、多响收付、杠收付、合并零净值和十连胡：7/7 浏览器检查通过。早一轮失败包括旧断言等待5秒短于3倍慢放收付时点、短阶段被轮询间隔跳过、胡次数空格；按真实阶段采样和新文案修正后重跑通过。浏览器取证按正常速度，不用这些修正声明观感通过；上述截图均另行实际查看。动作音/TTS实现未修改，局内无自由感言；**录制无音轨、未实听**。完整大厅实玩、五主题/小屏及导航继续包5。

#### llmAnime 字形材质补充反馈

用户指出 `18eb5b2` 的大三元/清一色/十三幺呈软糖或塑料浮雕感，本轮再次实际查看 1:16.5～1:19.8 的 `title-impact.jpg` 连续画面。按补充要求只改 llmAnime：换成饱满粗字面、收紧字距和长尾笔，深紫外轮廓、单侧短厚边，删掉围绕笔画的多圈白色倒角，改成两处小面积斜切高光；粉紫色保留，时钟/飞牌/光柱/其他主题不改。

先用大三元静态对比：[修改前](../../work/blood-flow-refinement/material-before.png)、[修改后](../../work/blood-flow-refinement/material-after.png)、[并排对比](../../work/blood-flow-refinement/material-comparison.png)。实际检查发现 SVG use 复用时只在祖先 CSS 设置字体未作用到克隆字形，随后改为在 llmAnime 字形直接声明字体，复核后才进入原速录制。旧细楷体长尾和多圈亮边样本保留作对照，不当新成果。

随后固定输入录制大三元的来源/角色/冲入/回弹/读稳/收付，并补看三种不同牌型同场：[正常速度录像](../../work/blood-flow-refinement/material-normal.webm)、[大三元读稳](../../work/blood-flow-refinement/material-motion-04.png)、[三响新字面](../../work/blood-flow-refinement/material-multi-after.png)。`material-motion-00～11.png` 与真实采样时间保留。静态与动态取证均已执行并目视查看；未实听音轨。该补充仍属于包3/4的视觉复修，包5继续，不重开规则或架构任务。

#### 执行包 5：正常对局、固定补验与回归（2026-09-05）

实现冻结版本 **`37043a3`**，含材质修复 `0df7e27`；另保留其他任务提交的听牌修复 `6eefce3`。前端服务为 4175。初次开局仅打出两张后受到开发热更新重载，明确不计为完整对局；随后冻结代码，重新从正常大厅选择血流、llmAnime、东风场开局。入口截图 [最终大厅](../../work/blood-flow-refinement/play-final-lobby-37043a3.png)。

**实际操作者为用户，Codex 旁观取证。** 用户已明确确认自己在操作；没有将其操作记为代理点击，没有注入牌墙、设置 paceMs=0 或开启引擎 autoplay。已观察东1局到东2局、再到东3局的正常推进。东2局开局翻精阶段四席承接积分为本家/右家/对家/左家 **3890 / 1390 / 1540 / 1180**，总分8000；这是东1局结束后的承接值，不伪称取得了东1局完整流水或最终胡次数。窗口由用户调整，最终大厅/开局取证帧为 **1271×916**，旁观连续帧为 **1261×916**；固定浏览器回归另用1280×720等明确尺寸。

旁观连续取证覆盖东2局实际 `local-2/round-2/batch/24～26`：左家点炮本家平胡 **+10/-10**；本家自摸 **+60，三家各-20**；右家点炮本家 **+10/-10**。实际看见来源、对应角色、飞牌、落楼光柱/粒子、前方主字、两行收付和下一操作交接；本家胡次数15→16→17，楼牌持续累积。证据：[连续帧](../../work/blood-flow-refinement/user-live-contact.jpg)、[按采样时刻回放](../../work/blood-flow-refinement/user-live-normal.webm)、[逐帧时间与cue记录](../../work/blood-flow-refinement/user-live-times.json)。回放由约330ms间隔的实际截图按真实时差组成，不加速、不插帧；**不是整局录屏，没有音轨**。

| 补验 / 检查 | 本轮实际结果与证据 |
|---|---|
| 吃、杠 | 正常对局未完整取到这两类动作的连续过程，另用真实引擎 `blood-flow-claims.html?paced=1` 固定输入补验；直接选择吃/杠，不先点过。吃后11张手牌可继续出牌；直杠先成组/收付再补白板，来源一家-10、杠者+10。`fixed-chi-*`、`fixed-kong-*` 帧和result.json保留，已目视查看；吃牌首轮加载未完成的画面已在等待3D就绪后重录。 |
| 高番、二响、三响、连续胡 | 本轮未把未采到的自然事件算通过；使用包3/4及材质复修的固定展示输入补验。每席不同主番、真实规则计算金额、同源提示一次，十胡积压合并与重复投影去重已核对。 |
| 五主题×三个视口 | 1280×720、844×390、568×320，各主题均检查普通/高番/三响、收付、全部按钮和听牌网格；后三者不是用“节点存在”代替观察，图像另行查看。[桌面](../../work/blood-flow-refinement/themes-1280.jpg)、[844](../../work/blood-flow-refinement/themes-844.jpg)、[568](../../work/blood-flow-refinement/themes-568.jpg)。 |
| 小屏实修 | 本家大字下移到手牌前方、离开桌心；侧席标题内移并位于头像下侧，对家标题/收付移到头像旁。三家两行卡分席显示，按钮仍完整。`568-llmAnime-high / multi / multi-payment / buttons / waits.png` 已逐张核对。 |
| 原交互与导航 | 同窗吃碰杠胡过、触屏选中保留、14/11/8/5/2间隙、结算→牌桌→返回结算、×不重弹、返回大厅和重新开局的现有浏览器回归通过。已目视查看本次重新生成的 [重开大厅](../../work/blood-flow-playability/restart-lobby.png)、[重新发牌](../../work/blood-flow-playability/restarted-east-one.png)、[小屏返回结算入口](../../test-results/blood-flow-table-return-568.png)。此为自动化实际界面回归，未冒称用户逐项验过所有路径。 |

检查记录：

- 全量 `vitest run src` 最终 **104文件通过、1跳过；1119条通过、2跳过**。首轮发现既有听牌样式使用按视口的媒体查询违反项目响应式合同，改为同条件容器查询后定向2/2，再全量重跑通过；未改听牌数据或交互。
- 七组原浏览器回归首轮 **44/46**：一个首个音频样本播放时间未推进；一个旧立绘断言要求“主字已出现时角色仍在”，与新阶段交接冲突。前者原样重跑通过，后者改为角色先出现、主字阶段角色已退，两项重跑 **2/2**。原飞牌、跨层、去重、音频路由、结算和重开均保留。
- 新四状态/来源/金额/十连胡与收付组 **7/7**；五主题三尺寸首轮14/15（桌面jade首屏加载超过旧5秒等待），调整取证就绪等待后该项和材质变化的llmAnime三个尺寸 **4/4**；小屏位置修复后的五主题两尺寸 **10/10**；容器查询后568 llmAnime再次 **1/1**。不把不同批次次数相加冒称独立覆盖数量。
- 类型检查与最终 `npm run build` 通过。日志在 `work/blood-flow-refinement/unit-final.log`、`build-final.log`。所有 `work/` 证据为本机忽略产物，没有上传或随Git提交。

未验证范围：**人耳实听、带音轨整局录像、所有正常局的逐帧全程观察、真实LLM局末感言及全部声音设置组合**。本轮只保留/检查现有音频与主题准入路由，没有新增报声或胡牌感言。没有同步vibehub、迁移联机层、开发后端或发起真实P2P连接；全量前端命令包含仓库现有模块单测，不将其称为P2P实玩验收。用户指出的旧位置和软糖材质版本均保留为未通过样本；新版本已有复核证据，不代替用户对最终观感的认可。

| 本轮提交 | 内容 |
|---|---|
| `ff0658a` | 实际看片与取舍记录 |
| `023cb52` | 信息减负与四种默认状态 |
| `0cf3bc3` | 通用字面与短重拍时序；位置/材质随后被用户指出并修复 |
| `18eb5b2` | 本家位置纠正、分席来源/动作/主番/收付、多响与重复事件 |
| `0df7e27` | llmAnime饱满粉紫字面、深紫轮廓、短厚边与局部高光 |
| `37043a3` | 小横屏位置、响应式合同及尺寸回归；正常实玩冻结版本 |

| 执行包 | 必须留存的结果 |
|---|---|
| 信息减负 | 等待、选牌、可胡、收付四种默认状态截图；合法按钮仍齐全，预览与实际结果区分，完整明细可查；同一席位同笔金额仅一处临时反馈 |
| 单次高番 | 同一事件的起势、冲击、回弹、读字、收付连续画面，以及正常速度播放；主字不被角色遮住，字形与空间层次可辨，普通光效基线保留 |
| 放枪 / 多响 | 本家点炮胡、本家放枪、两响、三响的来源与赢家对应；每位赢家不同主番和真实金额，公共提示 / 角色 / 主番错峰，收付不重复 |
| 整局 | 从正常大厅开始的一局，记录主题、实际视口和代码提交；未自然触发的动作另用固定输入，明确区分；结算回桌、返回大厅和重新开局仍可用 |

五主题至少覆盖普通 / 高番 / 多响；小横屏重点检查主字、立绘、听牌提示和操作区域。调整演出时长后，补查输入何时恢复与倒计时何时开始。静音、TTS 不可用时不得锁住操作；实听未完成单列。此处不要求本阶段完成 P2P 验证。

## 2026-09-05 master 单机可玩性复修

开始状态：干净的 master `26ff022`。代码提交：`95ed115`。本阶段只做前端单机；按用户最新要求未同步 vibehub，未迁移或验收真实 P2P，未修改后端、番型、底分、封顶或锁手规则。

### 实际操作与观察

Codex 通过浏览器正常大厅选择血流，使用默认速度完整打完东一局（19:20–19:34，包含操作判断和取证等待，不是速度基准）。没有替换正常对局牌墙，没有设置 `paceMs:0` 或开启引擎 autoplay；锁手后的重复操作根据界面可见的胡按钮或唯一可操作摸牌点击。浏览器视口为 **932×582**，主题为 jade。

| 项目 | 实际观察与结果 |
|---|---|
| 普通摸打 | 三家摸牌、出牌、落牌可分别观察；摸牌显示在右端，阶段结束后本家手牌才恢复操作 |
| 吃碰杠 | 本家自然吃牌，11 张手牌成组后可继续出牌；其他座位自然触发杠。另用真实引擎固定输入复核直接碰、碰后补杠：第四张成组、收付后才补牌，再开放操作 |
| 同窗响应 | 自然出现“吃 / 胡 / 过”，直接选择胡；固定输入补验仍同时提供吃碰杠胡过，多吃法使用原选择器 |
| 连续胡与多响 | 本家第 1、2 次胡与右家分别形成二响，来源均为左家四条；之后继续自摸及点炮，没有结束牌局或重新亮开全部手牌 |
| 跨层 | 本家第 5 次自摸四筒落到第二层，右家也自然跨层；最后本家 10 张、右家 11 张均为三层，历史牌没有重新飞入 |
| 交回操作 | 本家第 5 次胡连续帧中，飞行 → 命中光柱 → 回弹读番 → 三付一收 → 下一家摸牌按顺序出现；演出时原手牌不可操作，之后只开放合法摸切牌 |
| 结算 | 四家按本家/右家/对家/左家：胡 **10 / 11 / 2 / 0** 次，分数 **2070 / 2190 / 1940 / 1800**，净变化 **+70 / +190 / -60 / -200**，合计为零 |
| 返回 | 实际点击“查看牌桌 → 返回结算”，再用关闭按钮收起并重新打开；结果未变化，最后从结算返回正常大厅 |

实玩发现的额外遮挡已在同一代码提交修复：`userDiscardWaits` 不再直接引用当前听口，避免每个窗口自动展开大面板。补杠后的界面已实际确认听牌入口默认收起；触屏选牌和同窗重复投影的选中保留另由回归覆盖。整局早期证据保留发现问题时的画面，不虚称这局录制发生在所有修补之后。完整三类听牌提示仍属 G04 后续任务。

### 连续帧与场景补验

本地证据入口：[实玩帧与回放](../../work/blood-flow-playability/index.html)。这些文件在被忽略的 `work/` 中，随本机交付，**没有随 Git 提交或上传**。

| 证据 | 性质及覆盖 |
|---|---|
| [本家首次胡 / 二响连续帧](../../work/blood-flow-playability/play-10-own-hu-contact.png) | 上述真实对局，单张来源分向两座楼 |
| [本家第五次胡跨层](../../work/blood-flow-playability/play-15-2-contact.png) | 上述真实对局，自摸三付与下一动作交接 |
| [吃牌后的连续帧](../../work/blood-flow-playability/play-02-chi-contact.png) | 上述真实对局，成组、整理与右端间隙 |
| [补杠到补牌](../../work/blood-flow-playability/paced-added-kong-contact.png) | 真实规则引擎固定输入补验；本地夹具逐阶段投影，不代表真实 P2P |
| [普通胡基础反馈](../../work/blood-flow-playability/jade-normal-final-contact.png)、[高番](../../work/blood-flow-playability/jade-high-final-contact.png) | 现有演出夹具；普通光柱/粒子已可见，高番 2600ms，独立于自然对局结果 |
| [五主题对照](../../work/blood-flow-playability/five-theme-contact.png) | 五主题普通胡及高番均有完整连续采样，932×582；保留各主题原色彩、字体、粒子及角色表现 |
| [568×320 高番连续帧](../../work/blood-flow-playability/verified-568-llmAnime-high-contact.png) | 独立 Chromium 确认实际 `innerWidth/innerHeight`；另保存 844×390，jade / llmAnime 两主题普通胡与高番帧 |
| [最终结算](../../work/blood-flow-playability/final-settlement.png)、[公开流水](../../work/blood-flow-playability/final-ledger.txt)、[返回大厅](../../work/blood-flow-playability/final-return-lobby.png) | 上述完整真实对局的实际界面 |

连续帧约每 180–280ms 一张，标签是采样起点后的实际时间。入口中的 WebM 按采样时间回放，没有加速、插帧或音轨；**不是完整整局录像，不用于证明帧率或实听**。`render-evidence.py` 和各 `*-times.json` 留在证据目录，可重新生成接触表与回放。一次 in-app 视口覆盖没有实际改变截图尺寸，对应 `small-*` 帧未计入小屏通过；最终只采用 `verified-*` 的真实尺寸帧。小屏夹具控制按钮曾挡住标题，已移开并重新取证；不是产品中的按钮。

### 检查记录及边界

- 定向规则/共享报牌/演出/轨迹/光效：5 个文件 **36 条通过**，包含阶段内拒绝操作、延后补牌、完整决策时限和重复推进不再计分/发牌。
- 全量 `node node_modules/vitest/vitest.mjs run src`：102 文件通过、1 跳过；1108 条通过、2 跳过，4 条共享投影兼容检查失败。原因是新增阶段字段以 `undefined` 键出现在旧快照中；改为仅本地阶段存在时才写入后，engine 与该兼容文件定向 **32 条通过**。未把这次单元检查称为 P2P 实玩或迁移完成。
- 原有浏览器组（按钮、间隙、结算、飞牌、光效、音频路由）先得 **36/39**；3 条旧飞行用例误把新的来源停留判成没运动。改为等待实际起飞后，飞行组 **7/7** 通过。
- 触屏重复投影新增回归后，按钮组 **8/8** 通过：同窗选择、11 张布局、选中保留、点击音一次。原 14 / 11 / 8 / 5 / 2 张与非血流对照、结算导航回归保留。
- 小屏连续帧补验 `blood-flow.playability.spec.ts`：**2/2** 通过，并已逐帧观察；不是仅以断言存在为验收依据。
- `npm run build`（含类型检查）通过；Git 差异空白检查通过。前端服务单独运行于 4173，浏览器命令设置 `E2E_REUSE_ONLY=1`，没有启动后端。
- 声音：只验证原动作资源 / 固定角色 TTS 路由、播放事件次数、静音和回退，**未实听**。保留原报声，未新增替代人声或双播。
- 本阶段可玩性实玩和以上指定视觉样本已复核；未声称用户已认可最终打击感，未声称五主题×全部尺寸完整矩阵、真人声音、模型感言、原玩法完整实玩或真实 P2P 已通过。

## 证据按版本看

2026-09-05 听牌提示缺失（G04）：原适配每换响应窗口就清空听口，仅本家存在可操作动作时重算，且 `userTingOptions` 恒为空；吃碰后没有摸牌标记时还会直接跳过计算。现由既有评分 worker 批量计算本家当前听口和每种合法弃牌听口，复用原评估器；按手牌/副露/精牌/锁手缓存，别人出牌只刷新可见余张，过期计算取消。界面显示“已听 / 可听”，恢复原弃牌箭头和悬停/选牌提示；轮到本家时也可打开当前听口，锁手仅标记实际摸牌。网格继续只显示倍数与余张，不自动展开长明细。

本轮 HUD 接线随并行提交 `023cb52` 进入 master，本次补齐相应计算、数据及回归。`handWaits.test.ts` 7 条通过（14/11/8/5/2 张、当前听口与弃牌预览分开、锁手同牌摸切）；`blood-flow.claim-actions.spec.ts` 10 条通过，涵盖碰后可听、出牌后无本家动作仍保留已听、同窗重复投影、锁手箭头及触屏选择。构建（含类型检查）通过。已通过真实规则引擎固定输入在浏览器操作并观察[碰后箭头](../../work/blood-flow-playability/ting-after-peng.png)、[选牌后的网格](../../work/blood-flow-playability/ting-selected-preview.png)、[别人回合的已听入口](../../work/blood-flow-playability/ting-other-turn.png)。这不是新增计分规则，也不视为本轮演出或P2P整体验收。

2026-09-05 中途退出后重开：旧血流 `returnToLobby` 未清空玩家，导致大厅背后的 HUD/3D 牌桌仍挂载；App 重开时重置就绪信号，旧牌桌却不再发送 ready，于是停在 136 张。现复用公共退场清理，旧玩家及牌桌正常卸载。新增 `blood-flow.local.spec.ts` 的 `restarts from the lobby after leaving an unfinished east match`，修复前实际失败在返回大厅后画布仍有 1 个；修复后通过（32.2s），正常点击大厅开始、发到 14 张并可出牌，牌数降至 13。首局通过保守恒的末巡输入进入真实结算以缩短准备时间；重新开局使用完整正常牌墙及默认开局时间线，没有用演出夹具模拟成功。已目视检查[退场后的大厅](../../work/blood-flow-playability/restart-lobby.png)及[重新开局后的东一局](../../work/blood-flow-playability/restarted-east-one.png)，类型检查通过。保留返回大厅按钮；此前仅验证“回到大厅”没有继续验证“再次开始”的记录，不再视作重开通过证据。

2026-09-05 胡牌次数移至头像：桌面放在头像卡片积分下，小屏放在卡片侧边，去掉层数文字。已目视检查 jade / llmAnime 的 [桌面](../../work/blood-flow-playability/avatar-wins-jade-1280.png) 和 [568×320](../../work/blood-flow-playability/avatar-wins-jade-568.png)；本家卡原本禁用指针事件，已为次数按钮单独恢复点击。初次回归发现小屏与操作区重叠，调整为卡片侧边后，座位换向及 0 / 1 / 4 / 12 / 40 / 80 次布局回归 3/3 通过，结算导航 6/6 通过。对应玩家头像与流水归属一致，类型检查通过；未改变实际盖楼或计分。

2026-09-05 听牌提示精简：按用户两张参考图，移除左侧逐牌金额说明，在牌面下仅保留“倍数 / 剩余张数”，每行最多四张，零剩余置灰。倍数使用自摸最终倍率（含自摸/硬胡等既有计算；无自摸结果时使用点炮结果），不显示单家分数。类型检查及差异空白检查通过；独立浏览器已观察 [1280×720](../../work/blood-flow-playability/compact-waits-1280.png) 和 [568×320](../../work/blood-flow-playability/compact-waits-568.png)，面板宽度分别 238 / 170px，无横向溢出。本项为显示精简，不重跑整局或改变 G04 尚未完成的三类提示范围。

| 记录 / 提交 | 已有证据 | 不能据此声称 |
|---|---|---|
| [首版验收快照](records/validation-v1.md) | E02～E09 的评分、守恒、单机、mock P2P、模型回退、主题及分支检查；各次命令和数量保留原记录 | 最新 HEAD 整体验收通过、真实 WebRTC / Relay 或真实模型体验通过 |
| [千局模拟](records/simulation.md)，`757dabc` | 实际 TypeScript 引擎、种子 1～1000、3772 条胡记录；其中独立首次成牌 807 次 | 3772 次独立成牌、AI 对抗强度或长期平衡已经证明 |
| [策略分布](records/strategies.md)，`b0ae8f3` | 三种策略各 100 局自我对局，种子 1～100 | 与千局报告组成 1300 个独立种子，或可推导 LLM 胜率 |
| [渲染测量](records/rendering.md) | 指定 Intel / Chromium 环境下五主题四尺寸的记录 | 其他设备长期帧率、打击感和声音已经达标 |
| E10 `455c411` | 当时记录 9 项导航 / 布局检查及类型、构建通过 | E15 的局末感言呈现已经完成 |
| E11 `5ccbd56` | 同一 cue 时钟、逐席冷却、合并金额和立绘接线检查 | 自动调用正确即代表用户认可观感 |
| E12 `64d0bd3` | 当时记录 7 项 GPU 飞行、来源和跨层检查 | 单张截图能说明完整飞行和落稳过程 |
| E13 `15a5b83` | 三种番型三响、四家收付、杠收支及合并零净值检查 | 卡片上的收入文字可以代替四席动态收付反馈 |
| 动画 / 间隙 `1898559` | 14 / 11 / 8 / 5 / 2 张位置及重建时保留动画的回归；详见原检查记录 | 普通出牌 / 吃碰杠的完整前后节奏已全部对齐 |
| G01 `7ca6dbb` | 7 项浏览器用例覆盖吃碰杠胡过、多吃法和触屏；当时全量单测 1103 通过 / 2 跳过，后增响应边界的 engine 21 条通过；类型和构建通过 | 已恢复听牌提示、普通气泡或其他 G 项 |
| E14 `845e943` | 原阶段记录 22 项 GPU / 实际播声检查通过，效果音与分档实现已提交 | 人耳实听已通过，或用户截图已认可动态冲击感 |

上表为已有记录的归并，没有把不同提交的检查次数加在一起。完整历史命令保留在记录文件和 Git；未来新结果应附新提交。

## 必须补的体验验收

| 范围 | 通过条件 | 记录方式 |
|---|---|---|
| 普通动作与手牌（G01～G04） | 对照非血流的摸打、吃碰杠顺序；同牌多操作同时可选；重复快照不打断选牌和动画；三类听牌提示完整 | 桌面 / 触屏录屏加相关交互断言，注明规则和座位 |
| 字、牌、光、角色的同一批次（E11 / E12 / E14） | 过冲后回弹可读，来源和落点明确，历史牌不重新飞入；获胜立绘匹配席位 | 同事件慢放与正常速度录像；标注录制提交、主题、视口、事件 ID |
| 四家收付（E13） | 一次自摸三付、点炮多响、抢杠及杠分各自对应真实金额；零净值合并仍说明来源 | 同批次从动作前到数字退出的录像和账本向量核对 |
| 画面可读性（E14 / E16） | 主番不与桌心方位 / 倒数相撞；本次结果与下一次预览分清；小屏不遮操作 | 五主题，1280×720 / 844×390 / 568×320；截图用于布局，录像用于时序 |
| 声音（E14-7 / G05 / G07） | 原动作人声与非人声重拍分别实际可听；各自次数正确，静音有效、TTS 不可用能回退；选牌和倒数反馈恢复 | 带音轨样片与实听记录；请求、play 事件及非零音量断言是辅助证据 |
| 发言与结算（E15） | 两个 LLM 主题有正常摸打 / 吃碰杠短句；局内胡牌无自由评价；局末在结算卡可见，回桌重开不再播 | 主题 × 局内 / 局末 × TTS 可用 / 不可用矩阵，包含单机和 P2P |
| P2P 与原玩法（E06 / E16） | 同步后原两模式正常，主题锁定与结算路径保留；真实链路独立验收 | 分别记录 master、vibehub、mock 与真实 SDK 的提交 / 环境 / 结果 |

用户最近两张截图的判断已记入[任务页的截图反馈](tasks.md#截图反馈如何落到-e14--e16)。它们不构成动态或实听通过记录。

## 复测入口

命令在相应分支的仓库根目录运行。这里只列入口，执行者应先确认服务、工作区及本次变更范围。文档整理本身只检查迁移、链接和路径，不需重新运行千局模拟。

| 范围 | 命令 / 用例 |
|---|---|
| 全量前端 | `pnpm test`；`pnpm build`（包含类型检查） |
| 规则与响应 | `node node_modules/vitest/vitest.mjs run src/game/variants/lotus/bloodFlow/engine.test.ts src/game/variants/lotus/bloodFlow/ai.test.ts` |
| 按钮与间隙 | `tests/e2e/blood-flow.claim-actions.spec.ts`、`blood-flow.common-hand-gap.spec.ts` |
| 结算与布局 | `tests/e2e/blood-flow.settlement-navigation.spec.ts`、`blood-flow.presentation.spec.ts` |
| 飞牌与收付 | `tests/e2e/blood-flow.flights.spec.ts`、`blood-flow.payments.spec.ts` |
| 光效与声音 | `tests/e2e/blood-flow.effects.spec.ts`、`blood-flow.audio.spec.ts` |
| 单机与模型 | `tests/e2e/blood-flow.local.spec.ts`、`blood-flow.llm.spec.ts` |
| 千局报告 | `node node_modules/vitest/vitest.mjs run --dir scripts blood-flow.sim.test.ts`，更新 `records/simulation.md` |
| 策略报告 | `node node_modules/vitest/vitest.mjs run --dir scripts blood-flow.strategies.test.ts`，更新 `records/strategies.md` |
| 分支同步 | master 提交后 `pnpm sync:vibehub`；源和目标工作区须干净，随后在 P2P 分支复测 |

只复测前端浏览器时，先启动前端开发服务，再复用该服务，避免自动拉起本轮范围以外的后端。例如服务已在 4173：

```powershell
$env:E2E_REUSE_ONLY='1'
$env:E2E_PORT='4173'
node node_modules/@playwright/test/cli.js test tests/e2e/blood-flow.claim-actions.spec.ts --workers=1
```

## 新结果的记录格式

每批追加：日期 / 任务编号 / master 提交 / vibehub 提交与同步结果 / 命令与退出结果 / 样本与环境 / 证据位置 / 视觉或实听人及结论 / 未验证范围。

`work/`、`test-results/`、`docs/evidence/` 是被忽略的本地产物区，路径存在不表示文件随 Git 交付。受控文档保留摘要、生成方法和实际提交；外部原视频只读引用，不因整理文档上传或移动。

## 本次文档整理校验

2026-09-05：13 份原文档 / 补丁已迁移，4 个常用旧入口保留跳转；新目录含 16 份 Markdown 与原补丁。118 个本地链接及锚点检查通过，E00～E16 任务条目、16 个番型定义、历史实验表格和原补丁均保留。两个报告生成脚本仅调整 Markdown 输出路径；没有改变模拟逻辑或实验数字。文档差异空白检查通过，未重跑游戏测试。

## 2026-09-06 公共表现收拢：包 1 基线核对

基线：master `806d1de`；开始时仅 README、rules、tasks、presentation、impact-video 五份方案文档有未提交改动，全部保留。本轮不操作 vibehub、P2P、后端。检查先使用 CodeGraph，再定位具体调用。

实际查看优先视频的四张全片概览，以及 discard-win、title-impact、self-draw、discard-loss、multi-summary、ending 连续帧（`work/video-reference-843049425/`）。覆盖 1:04～1:09.5、1:16.5～1:19.8、1:58～2:03.5、5:44.5～5:47、5:49.5～5:54、6:06～6:27；点炮收付尾段与 1:10 续行另补核。观察：来源落定和操作先出现，角色/胡字确认赢家，角色退去后主番冲入回弹，最后逐席金额；两响共享来源提示后分席角色和主番，局末先完成末张再亮牌。不采用视频的资产、舞蹈、经济系统和牌规。此处为视觉查看，未实听。

| 能力 | 基线入口、资源及生命周期 | 复用判断 |
|---|---|---|
| 普通动作 | HUD 的主题模板 + themeEventPresentation；llmAnime 使用 AnimeActionCue 的角色动作图、头像回退；原 Transition/CSS 控制出入，无声音调用 | 待将模板原样提取，普通调用保留 |
| 血流动作 | BloodFlowWinPresentation 由同一 cue 映射每赢家，角色已有 progress 接口；非二次元另写 blood-flow-action；HUD 按模式过滤所有胡 | cue 映射为必要适配；独立动作字及角色装配消除，明确批次拥有胡事件 |
| 光柱/粒子 | bloodFlowWinEffects 管理多个 createWinEffectPresenter，使用原主题默认资源；cue 改变/到期 dispose；镜头选一次最强反馈 | 已共用，保留 806d1de 默认光效及 reduced-motion |
| 飞牌 | 原 presenter smoothstep 插值/缩放；血流 sampleBloodFlowFlight 独立插值/弧线；tableTilePresenter 保存真实来源，director 隐藏记录直到 readable | 来源、楼位、隐藏/交接必要；运动归公共能力，普通默认采样须不变 |
| 时钟/销毁 | 唯一 director，DOM/3D 共用 cue；换局/主题清队列，恢复历史只摆楼牌；实例重建重新采样同一时刻 | 已共用，不另开队列 |
| 终局 | 血流胡仅新增批次，原终局 winEffect/revealHands/result 不由表现回调赋值；末张结束才进入结算 | 严禁把公共显示接到普通终局动作 |

冻结普通 1900ms、大番 2300ms、顶级 2600ms 为本轮起始参数，不声称为最终最优。对照录制脚本：`work/blood-flow-common-before.mjs`，产物 `work/blood-flow-common/before/`，五主题均为 1280×720。原版通过正常大厅进入并使用已有动作/胡牌实验入口，血流通过现有展示夹具；这是可重复表现输入，不是规则实玩证据。录制与视觉复核结果待下方追加。

包 1 结论：完成代码及视觉基线核对。五主题各一份普通/血流原速 WebM；jade、llmAnime 另补 `*-stable-original.webm` 固定 HUD 对照（首次大厅录制存在开局干扰，不能作为每个动作的完整证据）。实际检查动作、飞行及落定连续画面 `jade-blood-sequence.jpg`、`anime-original-sequence.jpg` 和固定阶段 PNG；原版 jade 有主题几何底饰与“胡”，血流仅简化“点炮胡”；二次元资源相同但装配入口不同。补抽原视频 1:07～1:10.1 和 5:47～5:48.6，确认两家金额后续行，见 `work/blood-flow-common/reference-*.jpg`。采用原模板和角色，不重做番型字。

回归：`E2E_REUSE_ONLY=1 E2E_PORT=4186` 下 effects + flights **14/14 通过**；`pnpm typecheck` 通过。只启动 Vite。原速录像已保存，视觉判断基于连续抽帧，不虚称用户已认可或声音已实听。进入包 2。

## 2026-09-06 公共表现收拢：包 2

基于包 1 `a957f95`：将 HUD 原主题动作模板提取为 `TableActionCue.vue`，双方调用同一模板和 AnimeActionCue（角色、动作图及失败回退保留）。公共组件不播放声音；血流按记录 ID 与 cue 进度传入赢家。HUD 明确 table / win-batch / terminal-effect 的事件归属；批次拥有所有赢家，恢复/积压不从旧单事件补播。删除血流简化动作字与独立角色装配，番型标题及金额展示保留。

飞牌抽取 `winningTileFlight.ts`：原 smoothstep、旋转及缩放默认曲线保持；公共 presenter 支持可选来源/目标姿态、绝对起飞/重拍/落稳时间。血流旧 sampler 只映射 cue 时间，不再计算独立曲线/主题弯曲。来源缓存、抢杠位、实例重建与 director 的 hiddenRecordIds/readable 交接保持；灯光 presenter 仍不再画一张牌，历史牌不飞，不改实体账或终局入口。

回归：公共动作 + effects + flights **16/16 浏览器用例通过**，含三赢家身份/唯一入口、五主题、13/25/41 次跨层、三种来源、历史恢复；运动/原胡牌 **9/9 单测通过**，其中逐 10ms 比较原版默认轨迹、旋转和缩放；类型检查及 diff 空白检查通过。

视觉：`work/blood-flow-common/after/` 保存 jade、llmAnime 普通/血流原速 WebM、draw/discard/added-kong 各 8 帧及实际采样时刻。已查看来源途中、落层光柱和主番连续画面；jade 恢复原主题“胡”装饰，llmAnime 角色使用同一资源，落层未见新旧两张重叠。`anime-action-probe.png` 单次输入确认原自摸立绘/字，连续切换原动作的截图可能仍处于 Transition 退场，不当作缺失。既有普通动作时间线不改。实际视觉检查为连续画面复核，未代替用户认可或声音实听。包 2 完成，节奏可读性进入包 3。

## 2026-09-06 公共表现收拢：包 3

基于 `af68353`，普通胡总时长仍 1900ms，阶段调整为 impact 720 / readable 900 / score 1500 / exit 1700；高番保持 2300/2600ms。原因：旧普通稳定读番只有约 300ms；新阶段在不延后下一操作的情况下给约 500ms 完全稳定阅读。角色区间起点改为 impact×0.25−40ms，普通约 440ms；主番入场在角色退出之后，飞行按同一 cue 约 540ms。不改摸打/副露 PACE，不隐藏合法按钮，不修改首胡锁手或 AI 控制。

G05：有效窗口最后三秒复用 `didu.ogg`，按窗口 ID 去重，离场清理；没有可操作窗口、尚未开放、无限 deadline 或 countdownEnabled=false 时不播。动作音仍经原 sound 出口。

回归：类型检查、5 文件 **47/47 单测通过**；按钮/间隙/听牌/收付/倒数/五状态 **21 项浏览器用例均已通过**（批次先 20 通过，来源逐阶段截图用例漏掉短收付期；改为浏览器 rAF 采集实际 DOM 金额后单独复跑通过，保留原速录像和可捕获的截图，不延长产品时长迎合截图）。覆盖 14/11/8/5/2 张、桌面与触屏、同窗五按钮、重复快照选牌、静默默认倒数和未开放窗口。

证据：`work/blood-flow-common/package3/five-states-jade.webm` 为等待→选牌→可胡→演出→收付原速片段；`normal-sequence.jpg`、`high-sequence.jpg` 已实际查看。`normal-timing.json` 记录真实 rAF：动作约 166～582ms，完全稳定主番约 932～1416ms，收付约 1533～1916ms（含一帧采样偏移），演出中预计收益重叠帧为 0。来源、动作、主番及收付可分辨；牌去楼位、金额退出后预览恢复。五状态原截图保留，声音仅路由/播放测试，实听仍待包 5。包 3 完成。

## 2026-09-06 公共表现收拢：包 4

基于 `d48c1bf`，未新增渲染分支或推翻已验收字形。先查看 llmAnime 单次本家高番连续帧，确认原自摸动作/角色→主番→三付一收；再检查本家点炮胡、本家放枪、他家自摸、两响和三个不同主番三响。放枪时本家无赢家角色；三响各家分别是清一色/大三元/十三幺，各付收归属正确，金额合计为零。

回归：公共动作/飞牌及五主题三尺寸 **24/24 浏览器检查通过**；director/持续楼牌/主题强弱 **54/54 单测通过**；类型通过。包括 13/25/41 次落层、三条显示牌共用一个来源、恢复不重播、另一席首次高番不被冷却吞掉、十连胡合并。没有层数截断。

视觉：已实际检查 `work/blood-flow-refinement/{1280,844,568}-{jade,rosewood,happyMahjong,llm,llmAnime}-{normal,high,multi,buttons,waits}.png` 中各主题代表高番/多响及按钮画面；桌面主番分席、光柱保留，小横屏五个按钮完整，主番未盖手牌或按钮。新增 `work/blood-flow-common/package4/{844,568}-high.webm` 小屏原速片段与 `*-actor-readable.png`（角色淡入完成后），确认真实赢家动作图和文字。来源/多响原速样片 `work/blood-flow-refinement/source-winners-normal.webm`，实际查看连续帧 `package4/sources-sequence.jpg`。原主题资源及普通基础特效保持。未收到用户新的外观认可，不将助手视觉检查写成用户认可。包 4 完成。

### 包 5：先行实玩与声音证据（G08 公共收拢前，dc2458d）

通过正常大厅选择血流、llmAnime，按实际可见按钮逐手操作完成东1局；未注入牌墙、未设置 autoplay/paceMs=0。助手点击吃、过和弃牌，过程中观察到自然连续胡、两响及本家首胡后的锁手。本家起初14张，吃后11张；由单张五筒听口改为五八筒听口，已听提示持续存在。实际视觉截取包括余37张的对家自摸角色、余15张的楼牌与手牌，以及全桌亮牌后的结算；截图和操作 AX 证据保存在本任务的浏览器工具记录中，不伪称完整录像已落盘。

牌墙耗尽后：本家 1860（-140，胡1次）、右家 2000（0，胡5次）、对家 2380（+380，胡14次）、左家 1760（-240，胡0次），合计8000；未自然出现杠分。结算→查看牌桌→返回结算→继续下一局已实际点击，进入东2局且保留累计分。单局实玩不能代替最终排名/缺失事件，后者由固定输入回归补验。

真人实听：用户在本任务明确反馈 `sound-review.mp4`「两类声音都清楚，无异常」。样片在 `work/blood-flow-common/package5/`，按普通自摸、点炮、高番、三响顺序，来自当前公共路径固定输入和真实浏览器音频输出；人声/重拍同录。`audio-events.json` 保存调用时刻，视频音轨按录制起点近似对齐，不声称毫秒级采集同步。该反馈不扩大为所有角色/供应商音色均已实听。

本阶段全量前端1121通过/2跳过、构建通过；包5浏览器批次28通过，1项旧TTS断言失败。定位：现有内容缓存移除了事件cacheIdentity、文本做NFKC归一化，503相同句子有30秒负缓存；测试改为按归一化文本识别且预期一次合成请求、三份结算气泡，单独复跑通过，保留缓存修复不改产品以迎合旧断言。

执行期间任务页新增 G08 公共能力/override 方案，已读取并保留其他任务的 README/audit/tasks 文档改动；下一步先完成 G08，再用用户授权的 `tmp/test-api-key.json` 进行真实模型验收。该文件已被 Git 忽略，密钥不回显、不进入提交及证据。

## 2026-09-06：撤回血流模型结算感言

按用户否决意见移除 createBloodFlowReactions 的 COMMENT 提示词和模型请求，直接复用原 winLines.ts 性格台词。保留主题限定、局末限定、同局去重、顺序播放及离场取消。血流有胡记录且本局净分非负时按最后一次真实胡来源选择原赢牌台词，其余使用输牌台词；全桌无人胡使用原荒庄台词，不伪造自摸来源。原固定文案未改。

定向 bloodFlowRuntime / winLines 两文件14项通过，类型检查通过；未实听声音，未完成新整局验收。此次不修改正在实施的公共 LLM 提示词与条件深思。

### 包 5 / G08：公共 LLM 收拢与真实模型验收

公共输入按两条实际调用链复用：原控制器与血流共同使用候选特征、公共快照、性格/事实/默认推荐提示词基础。血流只提供已校验的合法候选、锁手说明、16番型规则摘要、胡杠收益和实际局/庄信息；旧牌效求值继续复用，未算特征为 n/a/unknown。血流杠收益由自己的规则参数覆盖，不借旧模式的100分底分。原候选枚举/规则裁决不搬进血流。

`preparedDecision.ts` 统一供应商推理策略、条件深思配额、安全进度、取消清理与请求预算。删除血流2.8/4.5秒默认值，保留配置40秒或显式关闭超时；真实窗口较短时取交集（保留原250ms提交余量）。候选计算已用时间从实际请求预算扣除；默认40秒档不会因准备耗时几十毫秒被误判不支持深思。即使供应商Promise不响应取消，安全进度也能退出；迟到结果仍受窗口/epoch校验。

同时整合执行期间更新的用户约定及其既有代码：局末只复用原 `winLines.ts` 性格台词，按本局净结果及实际胡牌来源映射赢/输，无人胡才荒庄；不再生成 COMMENT 模型请求。普通发言继续原频率/事实过滤与TTS，吃碰杠不另播动态TTS，胡牌自由评价为零。

验证：全部前端 **1127通过 / 2跳过**，类型与构建通过；LLM/本地真实worker/五主题气泡与局末交接 **13/13浏览器用例通过**。新增回归覆盖共有性格指导、合法默认推荐、公共快照不带对手暗手、真实60分暗杠收益、未知特征、配置/窗口预算、每席深思配额、短窗口不升级、准备耗时不吞档位、无响应请求取消清理。旧LLM控制器77项用例通过，原两模式的条件推理及TTS回归保留。

真实配置：使用用户明确授权的 `tmp/test-api-key.json`，只在临时浏览器上下文内使用其中 DeepSeek `deepseek-v4-flash` 和通义千问 `qwen3.7-plus`，没有回显/提交Key。固定普通出牌和合法绿一色首胡输入，由真实客户端向配置中的供应商请求，选中动作再交真实引擎提交并检查守恒。两供应商均出现合法出牌、确认后普通短句；高番均实际选择胡、单家640、胡后自由发言0。千问高番约29.2s，有共享条件深思与固定安全进度；DeepSeek有限预算高番最终复测约1.7s，reasoning=true、请求上限约39.9s，成功提交。共7次逻辑决策调用中有1次DeepSeek高番走回退，随后同场景复测成功；不将这些样本宣传为供应商100%成功率。

证据：`work/blood-flow-common/package5/real-model-results-before-admission-fix.json` 保留首次发现有限预算准入问题的结果；`real-model-deepseek-final.json` 保留一次回退；`real-model-deepseek-high-recheck.json` 为修复后高番成功复测。这里只存动作、耗时、预算、统计和固定安全进度，不存密钥或原始推理内容。配置关闭超时的 Infinity 在首份JSON中序列化为null，含义为不限本地超时，不是零预算。真实模型测试是固定规则输入，正常大厅完整实玩另见上节。

补充实听：用户确认 `common-sound-review.mp4`「前三段可听，最后静音正常」，对应原摸牌音、原弃牌报一万完成路由、didu倒数提醒及静音；G05/G07本轮这组原资源/出口的实听闭环已补齐。无后端开发、P2P改造或vibehub同步。

### 包 5 最终收口（G08 提交 0640e19）

最后补齐五主题×1280×720 / 844×390 / 568×320 的本局/最终/明细/牌桌返回矩阵：15/15通过；同批原广麻开局与胡后结算、旧翻精开局3/3通过，共18/18。旧翻精测试原名称会同时匹配新增血流选项，已收窄到准确玩法名。`package5/{width}-{theme}-{round,final}.png` 为完成入场后的静态布局，已实际查看五主题小横屏最终排名和本轮正常大厅桌面结算；先前入场淡入截图不作为稳定可读性判断。

五包交付记录：包1 `a957f95`，包2 `af68353`，包3 `d48c1bf`，包4 `dc2458d`，包5内G08独立提交 `0640e19`；包5最终导航矩阵/状态更新随本条所在提交。前端完整1127通过、2跳过与构建成功；关键自动化、固定输入视觉、正常一局操作及用户真人实听分别在以上各节列明。两份声音样片均获得用户确认；真模型固定输入包含成功及一次实际回退，均如实保留。

本轮完成范围为master前端。没有同步vibehub、改造P2P或开发后端；没有部署。`work/`样片和测试密钥文件是本地被忽略产物，提交不携带它们；本地文件存在不代表随Git交付。未作“全部云端角色TTS均可用”或“用户已认可全部外观”的扩展结论，这些不替换本轮实际获得的证据。

## 2026-09-06 公共 TTS 中点恢复（G07 / G08）

基线 `eed865c`。此前台词在 `apply → observe` 后才播放，不能控制出牌时刻。本轮抽取原 runtime 的单次播放/气泡交接为 `decisionSpeechPlayback.ts`，原两模式与血流共同调用；继续使用 LocalTtsClient 内容缓存、原音色路由和 useAudio 实际媒体中点 Promise，不增设一段估计动画等待。

血流仅普通弃牌走提交前台词：频率/事实过滤后取用一次，playing 显示气泡，媒体中点返回后重验 epoch、窗口、deadline，再提交原合法动作。被取用的台词不再由确认快照重播。吃碰杠仍等公共动作确实执行，胡/强制动作不新增请求或自由台词，局末继续原 winLines。中点后音频尾部保持播放；离场/换局/主题变化可立即取消。修复 useAudio 在中点过早移除 abort 监听，监听保留到实际结束；取消的合成不进入30秒失败缓存，真正失败的负缓存保留。

验证：

- 4文件47项相关单测通过，含原 runtime 中点行为、单次台词取用、确认不重播及离场后同句可重新合成。
- 9类浏览器媒体/命令边界通过：成功、初始静音、合成503、媒体404、合成中离场、播放前半离场、中点后尾音离场、播放中静音、窗口在合成期间过期；同时模型/五主题气泡/局末交接9项通过。批次先17通过，过期用例原先等待已被取消的音频达到0.5秒而超时；改为明确保持合成至权威窗口过期再交回结果，单独复跑通过，没有放宽生产等待或deadline。
- 中点用例实际解码播放4秒WAV，真实worker收到唯一弃牌命令时 currentTime=2.125286秒、duration=4秒，合成次数1；在合成完成前及音频0.5秒时均无弃牌命令。证据 `work/blood-flow-tts-midpoint/success.json`。过期后无旧命令、无迟到播放，见 `deadline.json`。测试冻结首笔提交后的其他bot视图，隔离首笔实际命令和尾音，未用计时器伪造playing或中点。
- `pnpm test`：1129通过、2跳过；`pnpm build`（含类型）通过。Git差异空白检查通过。

本轮使用可控合成/失败响应及真实HTMLAudio验证时序，没有把测试音频称作真人TTS实听，也未改音频资源、后端、vibehub或P2P。此前两份资源实听记录保持其原范围。


## 2026-09-06 公共右手角布局

### 基线与范围

- 从 master `c273e67` 开始。开始时只有 `tasks.md`、`design/presentation.md` 的新增布局需求未提交；保留这些内容及此前五包成果，不重做五包。
- 本轮只修改本地公共前端。未改规则、计分、后端、P2P、两个后置决策话题；按本轮明确范围不执行 vibehub 同步。
- 实际看过 `work/video-reference-843049425/self-draw.jpg`、`multi-win-late.jpg` 连续帧，并从原 MP4 截取 118–123.5 秒、349.5–354 秒，在本地复核页以 1 倍速播放抽看。采用固定角落、首张/后续楼位连续性，不采用手模型、花卉、直播元素或参考牌规。

### 公共实现

- `src/game/core/presentation/tableLayout.ts`：公共局部右轴/向外轴、副露轨道、暗手让位、牌宽/横置宽/组距、固定角落、层间距和完整区域取景。角落由既有 `wallStackSlot` 的固定几何导出，不读取剩余牌数。半牌深试调不能避开补杠附牌，最终四家统一后移 1.1 世界单位，保留原排列轴。
- `tableTilePresenter.ts` 与 `winEffectPresenter.ts`：副露跨度和抢杠来源统一使用同一牌宽与中心偏移；横置来源牌、底边补偿、补杠附牌和普通摸打/吃碰杠时长保留。暗手从实际轨道末端计算让位，不再重复三组旧起点。
- `winDisplayLayout` 委托公共布局；`bloodFlowWinPiles` 取消整层居中偏移，首张和普通单张完全同坐标。桌面4张/层，小屏3张/层，层高仍 .46，无层数截断。所有 viewer/seat、1/4/5/13/25/41/80 张的容量与首张/跨层断言覆盖。
- 光柱、粒子、公共飞牌目标、最终静态牌和常驻楼继续消费已有布局结果；复用 TableActionCue、winningTileFlight、winEffectPresenter、原时钟。为容纳完整角落与高楼扩大公共相机取景，世界牌体尺寸不变；新增层导致取景变化时，自摸屏幕来源重新投影，修复了本轮实际捕获的跨层来源漂移。
- HUD 操作区使用中央空位，三/四副露时放到短手牌旁；小屏预览移入右侧空白栏，保留原详情入口。对家身份横排并避开顶栏。主番/飘分演出位置及头像次数入口保留，不迁入胡牌角落。

### 证据与结论

- 同输入前后：`work/common-corner-layout/before/` 与 `delivered/`，jade × 三尺寸 × 0/1/4 副露 × 1/4/5/13/25/41 张；输入脚本 `work/common-corner-capture.mjs`。28 张晚局牌河与补杠附牌同场。该输入是占地压力夹具，不冒充物理牌数合法的真实整局。
- 五主题最终静态矩阵：`work/common-corner-layout/matrix/`，15 个主题/尺寸组合，每组18张血流状态、12张普通四家单胡，共450张截图；浏览器用例 `common-corner-layout.spec.ts`。原始全尺寸图保留，复核拼图为各主题 `*-review.jpg`。
- 原速录制：`work/common-corner-layout/motion/{1280-jade,844-rosewood,568-llmAnime}.webm`，依次自摸、点炮、抢杠、三响；未传 motionScale、未改时钟。`work/common-corner-layout/review.html` 为本地播放页，原速播放抽看并结合160ms连续截图核对来源、落稳与最终牌交接。
- 正常大厅普通玩法补看：`work/common-corner-layout/ordinary-live.png`；普通手牌间隙、动作及单胡分牌以现有公共路径测试回归。本轮未重新打一整局，不能替代此前五包的整局记录。
- 试调问题保存在 `trial/`、`framed/`、`hudtrial/` 等目录：旧楼遮牌河/楼顶出画、低楼撞操作区、预览文字溢出及顶栏身份重叠均作为失败样本保留，不作为通过截图。

验证命令与最终结果在本节末更新。自动化通过不替代视觉证据；已抽看五主题三尺寸的代表画面、同输入前后图与原速片段，不声称逐帧人工审阅全部450张。高楼取景下桌面牌在小屏更小，保留完整楼层优先，用户主观观感尚未另获确认。声音资源、TTS中点与演出时长未修改，本轮不扩大为实听或真实联机验收。

最终检查结果：

- `pnpm test`：107个文件通过、1个文件跳过；1160项通过、2项跳过。
- `pnpm build`（包含 `vue-tsc --noEmit`）：通过；仅保留构建体积提示。
- 公共动作、手牌间隙、五主题光效、来源飞牌与布局矩阵批次：33项通过。最终内侧锚点调整后，15项完整矩阵及7项飞牌再次通过（22项）；小屏预览另补边界/内容溢出断言，五主题通过。
- 几何回归覆盖首张位于两面墙中心线内侧、整层不压任何活牌墙、四副露及补杠附牌不相交、实际轨道让位和无层数上限。
- 已实际查看前后拼图 `work/common-corner-layout/comparison.jpg`、五主题三尺寸代表图及 `matrix/boundary-review.jpg` 的4/5/13/25张×0/1/4副露画面。被检查的代表状态中，角落牌/楼顶完整、操作区与手牌可用，晚局牌河不再被旧楼位占用。
- 未验证：真实手机硬件/触控、所有450张截图的逐张人工复审、整局重新实玩及本轮声音实听。上述不以自动化通过替代；P2P、后端与同步按本轮范围未执行。


## 2026-09-06 恢复固定视角与平面重排（未完成整体视觉验收）

用户明确拒绝 e2f472b 的抬高/拉远相机。该提交的整体视觉完成结论不再作为当前需求完成依据。

- 已删除 `winRegionFrame`、随楼高扩大取景及 lookAt 高度偏移。相机恢复 c273e67 的主题基准：普通四主题 [0,17.2,11.8]、FOV39；llmAnime [0,20.8,14]、FOV34。保留原响应式FOV及原胡牌震动，不通过全场缩放替代相机缩放。调试数据改读相机实际位置。
- 副露按原排列方向后移半牌深 .47。四角牌堆向内填充，首张锚点固定；晚局十张宽牌河改为居中，腾出各家右手角空间。新增公共 `discardTileLayout`，显示与来源位置共同消费，前18张和普通摸打时长保持不变。
- 平面相交断言覆盖全活牌墙、四副露和补杠附牌、四家28张晚局牌河及完整4张楼层，已通过。普通胡与血流首张、持续盖楼和每层4/3张仍保持。
- 本轮重新查看参考 `multi-win-late.jpg` 连续帧。参考画面显示固定角落、更薄的视觉层间距及较收敛的牌河；没有展示四家同时41张，不据此声称已验证该极端容量。
- 原模型层距 .46 保留：小屏41张对应14层，顶层基准 y=.31+13×.46=6.29。固定原相机下远端楼顶仍出画；568×320的25张样本也已确认顶部裁切。这是明确未完成项，不能用相机断言或容量断言通过替代视觉通过。
- 已询问是否允许仅调薄胡牌展示牌及层距，保留牌面宽高、每层张数和总张数。因为此前用户明确要求保留尺寸，未收到回答前不执行该改变；不截层、不删牌、不再改相机。
- 图片证据：`work/common-corner-layout/fixed-camera-baseline/` 为只恢复相机的问题基线，`fixed-planar-controls/` 为平面重排后的低楼，`fixed-planar-tall/` 为原厚度高楼未通过样本；新五主题三尺寸矩阵另存 `work/fixed-camera-layout/matrix/`，不覆盖上轮对照。
- `pnpm test`：1160通过、2跳过；`pnpm build` 包含类型检查通过。完整视觉验收仍未完成，声音、后端、P2P及vibehub同步均未扩大范围。

固定相机复修自动化结果：手牌间隙2项、飞牌7项、五主题三尺寸矩阵15项，共24项通过。矩阵新增实际相机位置断言，覆盖1至41张及普通单胡；截图中的高楼裁切仍按视觉失败记录。


## 2026-09-06 按确认范围：原HUD与四角直排后移

本轮以 master `e487b1a` 开始，原有 `design/presentation.md` 未提交需求保留。用户明确：采用认可截图的四角直排基础，楼再向各自玩家侧后移；相机、对家头像、局名、本家操作区保持原位；高楼出画不再处理。此前“等待调薄授权”的条目由这次决定关闭，不再压缩牌厚或层距。

- `src/style.css`、`GameTableHud.vue` 已与 `c273e67` 逐文件比较一致：撤销操作行及预览区额外定位，去掉失效的副露数量属性。头像和局名样式也保持该基线。相机文件与本轮开始前完全一致。
- 公共 `tableLayout.ts` 采用认可截图的直排方向和副露轨道（1.1后移值）；胡牌首张与整层继续共用布局，四家的后移投影均大于 .5 世界单位。为预留完整四列占地，角落内还保留固定的横向余量。没有斜排、缩牌或截层。
- 将既有 `tableLiftSlots` 纯几何移到公共 presentation 层，Three.js 原入口重导出；机械口尺寸、牌山位置和桌布绘制均未改。楼位直接由完整开口的端点/边界导出，测试计入边框外 .08 的安全余量。
- 后侧楼位与相邻亮手牌的间距由公共 `concealedSideX` 计算，普通弃牌来源也消费该位置；本家2D手牌和HUD不动。四副露/补杠、28张晚局牌河、侧家14张亮手的占地均留回归。
- 普通单张与血流首张一致，桌面4张/小屏3张每层、.73列距、.46层距保持。飞牌/光柱/粒子仍走原公共布局与时钟，不改音效、TTS或规则。

证据：同输入前后 `work/common-corner-layout/outward-before/`、`outward-after/`；五主题×三尺寸的各张数、0/1/4副露、普通四家单胡及空墙画面在 `work/outward-corner-layout/matrix/`。录制 `work/outward-corner-layout/motion/` 三尺寸原速自摸、点炮、抢杠及多响；在本地 `review.html` 原速播放抽看，结合连续截图检查交接。压力夹具并非真实牌数合法的完整一局。

视觉口径：检查的是直排位置、机械口/牌墙间距、原HUD恢复及共享飞牌落点。固定相机下的极端高楼裁切按用户决定保留，原视口边缘压力画面也不通过缩放掩盖；不宣称所有极端组合都完整入画。真实手机硬件、重新完整实玩一局及本轮声音实听未进行。后端、P2P、vibehub同步仍不在范围内。
最终回归：全量单测1163项通过、2项跳过，类型检查及构建通过；本轮普通动作/手牌间隙/布局/飞牌批次26项通过，最终位置的15项五主题三尺寸矩阵与7项来源/跨层飞牌复跑22项通过。另逐文件确认 `src/style.css`、`GameTableHud.vue` 与布局前 `c273e67` 一致，相机文件本轮无改动。已查看 `work/outward-corner-layout/comparison.jpg` 和五主题的 `*-review.jpg`；保留出画，不据此宣称所有540张矩阵截图都完整入画或逐张人工审阅。

## 2026-09-06 恢复原L型牌河

按用户要求撤销宽行居中：四家前三行每行6张、第4行起每行10张，所有行共用原来的左端起点（-2.5个牌距），新增四列只向各玩家右侧延伸。仅修改公共 `discardTileLayout` 的横向起点；楼位、机械口、相机、头像、操作区均未改。新增四家第4/5行左对齐回归，相关31项单测与类型检查通过；已查看桌面和小屏截图，证据在 `work/common-corner-layout/river-L-restored/`。此前“宽牌河居中”的记录为已撤销历史方案。
